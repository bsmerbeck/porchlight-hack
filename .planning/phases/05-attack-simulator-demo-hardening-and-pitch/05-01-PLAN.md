---
phase: 05-attack-simulator-demo-hardening-and-pitch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - functions/src/demo/resetDemo.ts
  - functions/src/demo/resetDemo.test.ts
  - functions/src/reports/writeFamilyReport.ts
  - functions/src/reports/writeFamilyReport.test.ts
  - functions/src/secrets.ts
  - functions/src/index.ts
  - scripts/demo-reset.mjs
  - package.json
  - pi/lamp.py
  - pi/test_lamp.py
autonomous: true
requirements: [RISK-04, DEMO-01]

estimate:
  tokens: 17000
  raw_tokens: 17000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Running `pnpm demo:reset` returns the demo to a clean state: every non-kept call doc is gone, any still-live call was actually hung up on Twilio (not just abandoned client-side), `prompts/*` is empty, `lamp/current` is idle, and `households/demo`'s already-enrolled passkey survives the reset untouched."
    - "Any call that reaches a terminal state (endedAt gets set) grows a plain-English `report` field on `calls/{id}` within ~30s, without a human writing it by hand."
    - "A person standing at the Pi with no laptop nearby can push the joystick and watch the lamp visibly change through all five states, and the center button still raises the family alert exactly as it did before this plan."
  artifacts:
    - functions/src/demo/resetDemo.ts
    - functions/src/reports/writeFamilyReport.ts
    - scripts/demo-reset.mjs
    - pi/lamp.py
  key_links:
    - "scripts/demo-reset.mjs -> resetDemo onCall (DEMO_TOKEN-gated) -> Firestore calls/prompts/lamp/households + Twilio forceEndCall for any still-active call"
    - "calls/{id} write with a newly-set endedAt -> writeFamilyReport onDocumentWritten trigger -> claude-sonnet-5 -> calls/{id}.report -> auto-mirrored to households/{id}/feed/{id} by Phase 4's existing mirrorActiveCallToLamp trigger (no new mirror code needed)"
    - "pi/lamp.py joystick handler -> same render function the /state HTTP handler already calls -> Sense HAT LED matrix"
---

<objective>
**As a** solo builder resetting between demo runs at 4AM or 4PM, **I want to** one command wipe every trace of the last run and have every finished call grow its own plain-English report automatically, **so that** three back-to-back demo reruns never show a stale call, a half-hung-up Twilio leg, or a blank report field on stage.

This plan delivers RISK-04 (post-call family report) and DEMO-01 (one-command reset), plus a bonus: the Pi's joystick gets a local "poke it and watch it react" demo mode so the lamp stays interesting even with no call in progress.

Purpose: every later plan in this phase (attack simulator, rehearsals) depends on resetting to a known-clean state between runs, and on every call automatically explaining itself to the family without a human writing summaries by hand.
Output: `resetDemo` (onCall, DEMO_TOKEN-gated) + `pnpm demo:reset`; `writeFamilyReport` (Firestore trigger); `pi/lamp.py` joystick demo mode + `--selftest`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@functions/src/screening/endCall.ts
@functions/src/lamp.ts
@functions/src/secrets.ts
@functions/src/index.ts
@functions/src/verification/verifyLink.ts
@pi/lamp.py
</context>

<tasks>

<task type="tracer">
  <name>Task 1: resetDemo callable + pnpm demo:reset (DEMO-01)</name>
  <read_first>functions/src/secrets.ts (existing defineSecret pattern used for elevenLabsKey/twilioAccountSid/etc.); functions/src/screening/endCall.ts (forceEndCall(callSid, sayLine) signature -- reuse it, do not reimplement Twilio hangup); functions/src/index.ts (export pattern for onCall functions); functions/src/verification/verifyLink.ts (existing timingSafeEqual shared-secret pattern -- mirror it for the DEMO_TOKEN check, never use `===` on a secret)</read_first>
  <files>functions/src/demo/resetDemo.ts, functions/src/demo/resetDemo.test.ts, functions/src/secrets.ts, functions/src/index.ts, scripts/demo-reset.mjs, package.json</files>
  <action>Add `demoToken = defineSecret('DEMO_TOKEN')` to secrets.ts following the existing defineSecret pattern exactly. Create `resetDemo` as an `onCall` Function taking `{ token: string, keepCallIds?: string[] }`. The FIRST thing the handler does is compare `token` against `demoToken.value()` using `crypto.timingSafeEqual` on equal-length buffers (reject on length mismatch before comparing, same defensive shape as `checkVerifyToken` in verifyLink.ts) and throw `HttpsError('permission-denied', ...)` on any mismatch -- this is the ASVS L1 gate and it must run before any Firestore read/write or Twilio call, not interleaved with the reset logic. Then, in order: (1) read every `calls/*` doc; for any doc whose id is not in `keepCallIds`, if it has a `providerCallId` and no `endedAt` yet, call the existing `forceEndCall(providerCallId, 'Demo resetting.')` wrapped in try/catch (a stale or already-ended Twilio call must never abort the reset), then delete the doc regardless of that outcome; (2) delete every doc in `prompts/*`; (3) set `lamp/current` to `{ state: 'idle' }`; (4) read `households/demo` and merge-write its baseline `name`/`members[].name`/`members[].aliases` back onto it WITHOUT touching any `members[].passkeys` array already present -- read the doc first and carry each member's existing `passkeys` field forward untouched, so an already-enrolled judge's passkey survives a reset between demo runs. Return `{ callsDeleted, promptsCleared }`. Create `scripts/demo-reset.mjs` at the repo root: reads `DEMO_TOKEN` from `process.env.DEMO_TOKEN` (sourced from `.env.local`), POSTs `{"data":{"token":...,"keepCallIds":[...process.argv.slice(2)]}}` to the deployed `resetDemo` Cloud Function URL using the same callable-protocol POST shape already proven live for `startPasskeyRegistration`/`answerVerification` in Phase 4, and prints the JSON result. Add `"demo:reset": "node scripts/demo-reset.mjs"` to the root `package.json` scripts.</action>
  <acceptance_criteria>`pnpm demo:reset` against the deployed project (with `DEMO_TOKEN` set locally) leaves zero docs in `calls/*` and `prompts/*` except any explicitly passed as keep-ids, `lamp/current.state === 'idle'`, and `households/demo`'s existing `members[].passkeys` unchanged. Calling `resetDemo` with a wrong or missing token throws `permission-denied` and performs zero Firestore reads/writes and zero Twilio calls.</acceptance_criteria>
  <verify>
    <automated>pnpm --filter functions test -- resetDemo</automated>
    <fails_when>any resetDemo.test.ts case fails, OR the token comparison in resetDemo.ts appears after the first Firestore/Twilio call in source order (grep confirms the timingSafeEqual check precedes the first `.collection(` or `forceEndCall(` occurrence), OR `pnpm --filter functions build` exits non-zero</fails_when>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: writeFamilyReport -- post-call plain-English report (RISK-04)</name>
  <read_first>functions/src/lamp.ts (the existing mirrorActiveCallToLamp onDocumentWritten('calls/{callId}') trigger -- confirm it unconditionally copies the full call doc to households/{householdId}/feed/{callId} on every write, since that is what makes "mirrored to the feed doc" automatic rather than something to hand-build in this task); functions/src/screening/runTurn.ts (existing Anthropic client instantiation pattern and the exact turns/risk field shapes to feed into the report prompt); functions/src/secrets.ts (the Anthropic key secret's exact export name)</read_first>
  <files>functions/src/reports/writeFamilyReport.ts, functions/src/reports/writeFamilyReport.test.ts, functions/src/index.ts</files>
  <behavior>
    - Test 1: a write where `before.endedAt` is unset and `after.endedAt` is set triggers exactly one `claude-sonnet-5` call and exactly one `calls/{id}.update({report: <string>})`.
    - Test 2: a write where `before.endedAt` was already truthy (report already generated, or any subsequent unrelated write to an already-ended call) makes zero Anthropic calls and zero Firestore writes -- this guard prevents the trigger's own write from re-triggering itself.
    - Test 3: a write where `after.endedAt` is still unset (call still in progress) makes zero Anthropic calls.
    - Test 4: if the Anthropic call throws, the function still writes a fallback `report` string (e.g. "Report unavailable -- see call transcript.") rather than leaving `report` missing or letting the trigger throw unhandled.
  </behavior>
  <action>Create `writeFamilyReport` as `onDocumentWritten('calls/{callId}', ...)`, binding the same Anthropic secret `runTurn.ts` already uses via `secrets:[...]`. Guard the whole body on `!before?.endedAt && after?.endedAt`. Build a prompt from `after.turns`, `after.risk` (score/tactics/claimedIdentity), and `after.outcome`, and call `claude-sonnet-5` with a plain `messages.create` (no structured-output schema needed -- this is prose) asking for 2-4 plain-English sentences covering: what the caller wanted, why it was flagged (or why it wasn't, if verified), and what Porchlight did. Write the result to `calls/{callId}.report`. Do NOT write anything to `households/{id}/feed/{callId}` directly -- confirm in read_first that the existing `mirrorActiveCallToLamp` trigger already unconditionally re-copies the whole `calls/{id}` doc, including this new `report` field, to the feed doc on this same write; adding a second mirror write here would be redundant duplicate-write code.</action>
  <acceptance_criteria>A call doc transitioning from no-`endedAt` to `endedAt` set grows a non-empty `report` string within ~30s against the live deployed project, and that string also appears on `households/{householdId}/feed/{callId}.report` with zero additional code in this task (proving Phase 4's existing mirror does the propagation).</acceptance_criteria>
  <verify>
    <automated>pnpm --filter functions test -- writeFamilyReport</automated>
    <fails_when>any of the 4 behavior tests fail, OR the guard allows a second Anthropic call on a doc whose `before.endedAt` was already truthy, OR `pnpm --filter functions build` exits non-zero</fails_when>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Pi joystick demo mode + --selftest + redeploy</name>
  <read_first>pi/lamp.py in full -- the existing state-render function used by the `/state` HTTP handler, the existing joystick event loop and the `/joystick` alert-flag mechanism for the center button, and the systemd service name -- and pi/deploy.sh</read_first>
  <files>pi/lamp.py, pi/test_lamp.py</files>
  <action>In `pi/lamp.py`, extend the existing joystick event loop (do not write a second, competing one) so up/down/left/right calls the SAME render function the `/state` HTTP handler already uses, cycling forward/backward through the fixed order `idle -> screening -> verifying -> verified -> scam` (wrapping at both ends) purely locally -- no HTTP call out, no Firestore, no bridge involvement. Leave the existing center-press behavior (raising the family alert via the existing `/joystick` flag) completely untouched. Add a `--selftest` CLI flag: when passed, the script walks all five states in order via that same render function, holding each for 2 seconds, then exits with code 0 without starting the HTTP server. Create `pi/test_lamp.py` using only the stdlib `unittest` + `unittest.mock` (Python 3.9, no pip installs): before importing `lamp`, inject a `MagicMock()` into `sys.modules['sense_hat']` so the import succeeds without real hardware; test that repeated up/down cycling wraps correctly at both ends and only ever calls the shared render function, and that `--selftest`'s state sequence covers exactly the 5 states in order. Redeploy via the existing `pi/deploy.sh`.</action>
  <acceptance_criteria>`python3 pi/test_lamp.py` exits 0 with no real Sense HAT attached. On the physical Pi, `ssh pi@169.254.10.2 'cd ~/lamp && python3 lamp.py --selftest'` (after redeploying) visibly cycles the LED matrix through all 5 states and exits 0 within ~12s; pressing the joystick center button still raises the family alert exactly as before this task.</acceptance_criteria>
  <verify>
    <automated>python3 pi/test_lamp.py</automated>
    <fails_when>pi/test_lamp.py fails or errors, OR `ssh pi@169.254.10.2 'cd ~/lamp && python3 lamp.py --selftest'` (run after pi/deploy.sh) exits non-zero, OR a diff of pi/lamp.py's joystick handler shows the pre-existing center-button alert branch was modified or removed</fails_when>
  </verify>
</task>

</tasks>

## Artifacts This Phase Produces

- `functions/src/demo/resetDemo.ts` -- DEMO_TOKEN-gated onCall reset (DEMO-01)
- `functions/src/demo/resetDemo.test.ts`
- `functions/src/reports/writeFamilyReport.ts` -- post-call Sonnet report trigger (RISK-04)
- `functions/src/reports/writeFamilyReport.test.ts`
- `scripts/demo-reset.mjs` -- `pnpm demo:reset` implementation
- `pi/lamp.py` (modified) -- joystick demo-mode cycling + `--selftest`
- `pi/test_lamp.py` -- hardware-mocked unit test for the joystick cycling logic

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anyone with the deployed Function URL -> resetDemo | Public HTTPS callable; only a shared secret gates a destructive operation (deletes call history, hangs up live calls) |
| calls/{id} write -> writeFamilyReport -> Anthropic API | Full call transcript (caller-supplied text) leaves our infra to Claude's API |
| Physical joystick on the Pi -> local LED render | No network boundary; physical presence only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Spoofing / Elevation of Privilege | resetDemo onCall | high | mitigate | `DEMO_TOKEN` shared-secret check via `crypto.timingSafeEqual`, checked first, before any Firestore/Twilio side effect; wrong/missing token -> `permission-denied` with zero side effects |
| T-05-02 | Tampering | resetDemo `keepCallIds` param | low | accept | Worst case for a single demo household during a hackathon is losing or keeping the wrong junk call doc -- no real user data at stake per PROJECT.md's no-real-seniors constraint |
| T-05-03 | Information Disclosure | writeFamilyReport -> Anthropic API | low | accept | Same data-flow class already accepted in Phase 2 for risk scoring (RISK-01); demo household only, no real PII |
| T-05-04 | Denial of Service | Physical joystick spam | low | accept | Local hardware only; worst case is a flickering lamp, no backend impact |

</threat_model>

<verification>
- `pnpm --filter functions test` passes (including the two new suites above).
- `pnpm --filter functions build` and `pnpm -r typecheck` both exit 0.
- Deployed `resetDemo` and `writeFamilyReport` confirmed live: a test call doc with a manually-set `endedAt` grows a `report` field; `pnpm demo:reset` against the deployed project leaves `calls/*`/`prompts/*` empty and `lamp/current` idle without touching `households/demo`'s passkeys.
- `python3 pi/test_lamp.py` passes locally; `pi/deploy.sh` redeploys `lamp.py`, and `--selftest` run over SSH visibly cycles all 5 states.
</verification>

<success_criteria>
One command (`pnpm demo:reset`) returns the system to a clean demo state (DEMO-01), every call that ends grows its own plain-English family report within ~30s with no human writing it (RISK-04), and the lamp is an interactive attraction on its own via the joystick even with no call in progress.
</success_criteria>

<output>
Create `.planning/phases/05-attack-simulator-demo-hardening-and-pitch/05-01-SUMMARY.md` when done
</output>
