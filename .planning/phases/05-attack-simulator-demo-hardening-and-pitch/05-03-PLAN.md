---
phase: 05-attack-simulator-demo-hardening-and-pitch
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - DEMO.md
  - scripts/demo-check.sh
  - PITCH.md
autonomous: false
requirements: [DEMO-02, DEMO-03, DEMO-04]

estimate:
  tokens: 9000
  raw_tokens: 9000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Anyone reading DEMO.md, without having built this system, can run the 3-minute demo start to finish, knowing exactly who presses what at each of the 7 beats, how to reset between runs, and what to do if venue Wi-Fi or the phone call itself dies mid-demo."
    - "scripts/demo-check.sh gives a single go/no-go answer before walking on stage -- site, Functions, Pi, and bridge all confirmed reachable in one run."
    - "The demo has run clean, end to end, 3 times in a row on venue Wi-Fi and 3 times on phone hotspot, with a recorded backup video in hand before 5PM."
    - "PITCH.md's traction slide carries the real signup count pulled live from stats/waitlist.count, not a stale or guessed number, and the FBI IC3 figure has been verified against its source before it goes on a slide."
  artifacts:
    - DEMO.md
    - scripts/demo-check.sh
    - PITCH.md
  key_links:
    - "scripts/demo-check.sh -> curl checks against the live site, a deployed Function, the Pi's /health endpoint, and a bridge liveness signal -> a single printed go/no-go table"
    - "DEMO.md's reset step -> pnpm demo:reset (05-01) -> DEMO.md's /sim fallback -> the existing browser simulator (Phase 2, DEMO-05)"
    - "PITCH.md traction slide -> stats/waitlist.count read live from Firestore/the landing page counter (Phase 1, LAND-03) at freeze time"
---

<objective>
**As a** solo builder pitching at hour 25 on four hours of sleep, **I want to** a single runbook that tells me exactly what to press, a single script that tells me go/no-go before I walk up, and a deck that already has the real numbers in it, **so that** three back-to-back live reruns on flaky venue Wi-Fi don't depend on my memory or my luck.

This plan delivers DEMO-02 (3 clean reruns on Wi-Fi and hotspot), DEMO-03 (recorded backup video before 5PM), and DEMO-04 (the 5-slide deck with real traction numbers).

Purpose: everything built in the previous four phases plus 05-01/05-02 is worthless on stage if the builder fumbles the run order, the Wi-Fi drops mid-call with no fallback, or the deck's numbers are stale by pitch time. This plan is the difference between "it worked in testing" and "it worked on stage."
Output: `DEMO.md` (the exact run script + reset + fallbacks + backup-video checklist); `scripts/demo-check.sh` (go/no-go health check); `PITCH.md` (5-slide deck with speaker notes and live numbers); a recorded backup video; 3 clean rehearsals on both networks.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@../PLAN.md
@.planning/ROADMAP.md
@functions/src/demo/resetDemo.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: DEMO.md run script + scripts/demo-check.sh (the runnable backbone)</name>
  <read_first>../PLAN.md's "Demo (3 min)" section (the 7-beat script this file must turn into an exact, role-annotated runbook); pi/lamp.py and bridge/*.ts or bridge/*.js (read whichever bridge entrypoint exists, to find its actual health/liveness signal and the Pi's real HTTP port for /health) -- do not guess ports/paths, confirm them by reading the real source</read_first>
  <files>DEMO.md, scripts/demo-check.sh</files>
  <action>Write `DEMO.md` at the repo root as the literal run script for the 3-minute demo, annotating who does what at each beat (Builder / Judge-with-phone / Judge-holding-grandma-phone), based on the 7-step script in ../PLAN.md: (1) voice already cloned -- state this is prep, not a live step; (2) prop phone rings, Porchlight answers, lamp pulses blue; (3) `pnpm demo:attack` (or the cloned-voice caller in person) runs the grandchild-in-jail script, live transcript + risk climbing on the projector; (4) the family member's phone (a judge) buzzes "Are you calling Grandma right now?", judge taps No; (5) lamp flashes red SCAM, call dropped, family alerted; (6) a second call, real family member, tap Yes with passkey, lamp turns green, call connects; (7) close on the live waitlist count + quotes + market + RI pilot ask. Include an explicit "Between Runs" reset procedure (`pnpm demo:reset [-- keepCallId...]`, confirm `scripts/demo-check.sh` is green before the next run) and two fallback sections: "If venue Wi-Fi drops" (switch the Mac to phone hotspot -- the lamp keeps working regardless since it is wired over ethernet, never Wi-Fi, per the locked architecture) and "If the phone call itself won't connect" (fall back to the `/sim` browser simulator from Phase 2/DEMO-05, driving the identical pipeline with no telephony). Add a "Backup Video Checklist" section: what to capture (the full 7-beat run, screen + phone), what tool to use (QuickTime screen recording + a phone camera on the prop phone and the verifier's phone), and where to save it. Create `scripts/demo-check.sh`: a bash script that curls the live site (`https://porchlight-hack.web.app`), a deployed Function (reuse the same reachability signal Phase 2 already established -- an unauthenticated `elevenlabsCustomLlm` request returning 401, not a connection error, proves Functions are up), the Pi's `/health` endpoint at whatever host:port was confirmed in read_first, and the bridge's liveness signal (confirmed in read_first -- if the bridge has no HTTP endpoint, check that `lamp/current`'s Firestore `updatedAt` is recent, or that the bridge process is running, whichever the actual bridge code supports), then prints a single go/no-go table (one row per check, PASS/FAIL) and exits non-zero if anything failed.</action>
  <acceptance_criteria>`scripts/demo-check.sh` runs in under 10 seconds and prints a clear PASS/FAIL table for site, Functions, Pi, and bridge. `DEMO.md` covers all 7 beats with an explicit actor for each, a reset procedure, and both named fallbacks (hotspot, `/sim`).</acceptance_criteria>
  <verify>
    <automated>bash -n scripts/demo-check.sh && test -s DEMO.md && grep -qi "reset" DEMO.md && grep -qi "hotspot" DEMO.md && grep -qi "/sim" DEMO.md</automated>
    <fails_when>`bash -n` reports a syntax error in demo-check.sh, OR DEMO.md is missing any of the reset/hotspot//sim sections, OR a real run of `scripts/demo-check.sh` against the deployed project does not print a PASS/FAIL table</fails_when>
  </verify>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: Three rehearsal runs on venue Wi-Fi and on phone hotspot (DEMO-02)</name>
  <read_first>DEMO.md (this task's own script, produced by Task 1)</read_first>
  <files>DEMO.md</files>
  <action>Run `scripts/demo-check.sh` before each attempt. Perform the full DEMO.md run 3 times in a row on venue Wi-Fi with `pnpm demo:reset` between each run, then 3 times in a row on a phone hotspot. Fix anything that breaks by editing DEMO.md directly (a missed beat, a timing issue, a wrong assumption about who presses what) so the document always reflects what actually works, not what was originally guessed.</action>
  <acceptance_criteria>3 consecutive clean runs on venue Wi-Fi and 3 consecutive clean runs on phone hotspot, each ending with the lamp reaching both red (scam) and green (verified) at the correct beats, with no manual intervention beyond what DEMO.md itself calls for.</acceptance_criteria>
  <verify>
    <automated>bash scripts/demo-check.sh</automated>
    <fails_when>demo-check.sh reports any FAIL immediately before a rehearsal attempt, OR fewer than 3 consecutive clean runs are completed on either network</fails_when>
  </verify>
</task>

<task type="checkpoint:human-action">
  <name>Task 3: PITCH.md deck + backup video + pitch rehearsal + 5PM freeze (DEMO-03, DEMO-04)</name>
  <read_first>PROJECT.md's Business Context (rubric weights, RFS alignment) and Context section (FBI IC3 2024 figure, "verify before it goes on a slide"); DEMO.md's Backup Video Checklist (Task 1)</read_first>
  <files>PITCH.md</files>
  <action>Write `PITCH.md` with exactly 5 `##`-level slide sections and speaker notes under each: (1) Problem + verified numbers -- re-verify the FBI IC3 2024 figure (~$4.9B lost by people 60+, up ~43% YoY) against its primary source before writing it down as fact, and note the verification date/source inline; (2) Live demo -- a one-line cue to the presenter, not a re-description of DEMO.md; (3) How it works -- the architecture in judge-friendly language (screener, risk score, family verification, lamp); (4) Traction -- pull the live `stats/waitlist.count` value as close to freeze time as possible (do not hardcode a number written hours earlier), plus quotes and pilot-interest notes gathered during the event per PROJECT.md's Active requirements; (5) Market + ask. Record the backup video per DEMO.md's checklist (full 7-beat run, screen + phone) before 5PM. Rehearse the pitch itself 3 times against a clock. At 5PM, declare and record the feature freeze (a dated line at the bottom of PITCH.md: "Feature freeze declared at <time>, no further code changes before the demo").</action>
  <acceptance_criteria>PITCH.md has exactly 5 slide sections with speaker notes, a traction number pulled at or after the last waitlist check before freeze, and a verified IC3 citation. A backup video file exists, was recorded before 5PM, and covers the full demo. The pitch has been rehearsed 3 times. A feature-freeze line with a timestamp is present in PITCH.md.</acceptance_criteria>
  <verify>
    <automated>test -f PITCH.md && [ "$(grep -c '^## ' PITCH.md)" -eq 5 ] && grep -qi "feature freeze" PITCH.md && grep -qi "IC3" PITCH.md</automated>
    <fails_when>PITCH.md does not have exactly 5 `## ` slide headings, OR no feature-freeze line is present, OR the IC3 figure was not re-verified (no citation/date noted next to it), OR no backup video file exists on disk before the recorded freeze timestamp</fails_when>
  </verify>
</task>

</tasks>

## Artifacts This Phase Produces

- `DEMO.md` -- exact 3-minute run script, roles, reset procedure, hotspot + /sim fallbacks, backup video checklist (DEMO-02 backbone)
- `scripts/demo-check.sh` -- go/no-go health check across site, Functions, Pi, bridge
- `PITCH.md` -- 5-slide deck with speaker notes, live traction numbers, verified IC3 figure, feature-freeze record (DEMO-04)
- A recorded backup video of the full demo (DEMO-03; stored outside git, path noted in DEMO.md)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scripts/demo-check.sh -> live infrastructure | Read-only health checks only; no destructive calls, no secrets embedded in the committed script |
| PITCH.md traction numbers -> the public landing page counter | Public, already-live data (Phase 1, LAND-03); no new trust boundary introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-08 | Information Disclosure | scripts/demo-check.sh committed to the repo | low | mitigate | Script reads any credentials it might need (none required for the checks described) from the environment, never hardcoded, so it is safe to commit and run from any machine |
| T-05-09 | Tampering | Stale/incorrect numbers on PITCH.md's traction slide | medium | mitigate | Traction number is pulled live as close to freeze time as possible rather than hardcoded early; IC3 figure is re-verified against its source before being written down |

</threat_model>

<verification>
- `bash -n scripts/demo-check.sh` passes; a live run prints a PASS/FAIL table.
- `DEMO.md` covers all 7 demo beats with named actors, a reset step, and both fallbacks.
- 3 consecutive clean runs on venue Wi-Fi and 3 on hotspot are completed and recorded as done.
- `PITCH.md` has exactly 5 slide sections, a verified IC3 citation, a live traction number, and a feature-freeze timestamp.
- A backup video exists and was recorded before 5PM.
</verification>

<success_criteria>
The full demo script runs end-to-end 3 times in a row without intervention on venue Wi-Fi and on phone hotspot (DEMO-02); a recorded backup video exists before 5PM (DEMO-03); the 5-slide deck is ready with the real traction numbers gathered during the event (DEMO-04).
</success_criteria>

<output>
Create `.planning/phases/05-attack-simulator-demo-hardening-and-pitch/05-03-SUMMARY.md` when done
</output>
