---
phase: 01-foundation-and-landing-page-live
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - apps/web/src/App.tsx
  - apps/web/src/index.css
  - apps/web/index.html
  - apps/web/src/components/Lamp.tsx
  - apps/web/src/pages/Landing.tsx
  - apps/web/src/pages/AppPlaceholder.tsx
  - apps/web/src/pages/Stage.tsx
  - README.md
autonomous: true
requirements: [LAND-01]

must_haves:
  truths:
    - "A visitor at `/` sees, on one phone screen and one desktop screen: headline, one-paragraph problem with the FBI IC3 number, 3-step how-it-works, an amber lamp illustration, the waitlist form, the live counter, and the RI Startup Week footer (LAND-01, D-09)"
    - "The FBI IC3 figure is visibly marked as unverified in the copy until the human confirms it (D-09)"
    - "`/app` and `/stage` load without a 404 and show a placeholder (D-08); the Hosting SPA rewrite handles deep links"
    - "The landing page still lazy-loads Firebase — the entry chunk contains no Firebase code (D-10 + 'keep JS light')"
    - "README explains run, build, deploy, and where secrets live"
  artifacts:
    - "apps/web/src/pages/Landing.tsx (Hero, Problem, HowItWorks, Waitlist, Footer sections)"
    - "apps/web/src/components/Lamp.tsx (SVG/CSS amber glow)"
    - "apps/web/src/pages/AppPlaceholder.tsx, apps/web/src/pages/Stage.tsx"
    - "apps/web/src/App.tsx (pathname switch: `/`, `/app`, `/stage`)"
    - "README.md"
  key_links:
    - "Landing.tsx renders the Plan 01 `WaitlistForm` and `LiveCounter` unchanged — the working path is reused, not re-implemented"
    - "App.tsx routes on `window.location.pathname` with no router dependency; `firebase.json` SPA rewrite (Plan 01) makes `/app` and `/stage` resolve to index.html"
---

<objective>
Turn the minimal `/` view from Plan 01 into the one-screen Porchlight pitch per D-09, add `/app` and `/stage` placeholder routes per D-08, and write the README with run/deploy instructions.

Purpose: This page is what gets QR-scanned in the room and posted with `?ref=` links for ~20 hours; it has to read in 20 seconds on a phone and stay light on bad Wi-Fi.
Output: Full landing page content and lamp illustration, placeholder routes, README.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-landing-page-live/01-CONTEXT.md
@.planning/phases/01-foundation-and-landing-page-live/01-01-SUMMARY.md
@apps/web/src/App.tsx
@apps/web/src/components/WaitlistForm.tsx
@apps/web/src/components/LiveCounter.tsx
@apps/web/src/index.css

**Design brief (CONTEXT "Specific Ideas", Claude's discretion for exact copy):** warm, calm, trustworthy — a porch light at dusk, not security-vendor red/black. Large type. Mobile-first: a single column that reads top-to-bottom on a phone; on desktop, hero text left / lamp right. No images, no web fonts, no router library, no analytics.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Landing page content sections and amber lamp illustration</name>
  <files>apps/web/src/pages/Landing.tsx, apps/web/src/components/Lamp.tsx, apps/web/src/index.css, apps/web/index.html</files>
  <read_first>
    - .planning/phases/01-foundation-and-landing-page-live/01-CONTEXT.md (D-09 content list is locked; "Specific Ideas" for tone; Deferred Ideas — no analytics, no email confirmation)
    - .planning/phases/01-foundation-and-landing-page-live/01-RESEARCH.md — "Pattern 4" (lazy Firebase must survive this task), "Pitfall 5" theme-vars note
    - apps/web/src/components/WaitlistForm.tsx and LiveCounter.tsx (reuse as-is)
    - apps/web/src/index.css (theme vars from Plan 01)
  </read_first>
  <action>
    Build `apps/web/src/pages/Landing.tsx` as a default-exported `Landing` component composed of five sections, mobile-first with Tailwind (`max-w-3xl mx-auto px-5`, `text-lg` body, `text-4xl md:text-6xl` headline, generous `py-12`). Content per D-09:

    1. **Hero**: headline "Porchlight — a guardian for your parent's phone." (h1), subhead one sentence: an AI answers unknown calls, catches scams and cloned voices live, verifies real family with one tap, and turns a lamp beside the phone red or green so your parent never has to read an app. Right/below: `<Lamp />`. Primary CTA `Button` that scrolls to `#waitlist` ("Join the waitlist"). Small `Badge` "Built in 24h — live demo Tue 9PM".
    2. **Problem** paragraph (id `problem`): one paragraph on elder phone scams citing the FBI IC3 number for losses reported by Americans 60+ (write the figure as "$3.4 billion (2023, FBI IC3 — verify)" with the literal parenthetical "verify" visible in the rendered copy, and a JSX comment line above it reading `TODO(human): confirm IC3 figure + year, then remove "verify"`). One line on voice cloning making "Grandma, it's me" calls convincing.
    3. **How it works** (id `how`): three numbered `Card`s in a `grid gap-4 md:grid-cols-3`: (1) "Screens" — unknown callers talk to Porchlight first; (2) "Verifies family" — a real family member taps Yes or No on their phone; (3) "Lights the lamp" — green for family, red for scam, no screen to read.
    4. **Waitlist** (id `waitlist`): heading "Protect someone you love", `<WaitlistForm />`, then `<LiveCounter />` directly below it (counter copy "N families on the waitlist" comes from LiveCounter). One line of reassurance: "No spam. We'll email once when Porchlight is ready for your family."
    5. **Footer**: "Built in 24h at RI Startup Week" plus "Providence, 2026". No external links required.

    `apps/web/src/components/Lamp.tsx`: a pure inline SVG (no image asset) — a simple lamp silhouette (post + shade as two paths in `currentColor` at low opacity) with a radial amber glow: a `<circle>` filled with a `<radialGradient>` from `oklch(0.85 0.17 75)` at full opacity to transparent, plus a CSS `animate-pulse`-style slow breathing via a `@keyframes porch-glow` rule added to `index.css` (opacity 0.7 -> 1 over 4s, `prefers-reduced-motion` disables it). Accept an optional `color` prop (default amber) so Phase 3's `/stage` can reuse it for red/green. `aria-hidden="true"`, fixed `viewBox`, `w-48 md:w-72`.

    `index.css`: append the keyframes + a `.lamp-glow` utility; keep Plan 01's amber theme vars. `index.html`: add `<meta name="description" content="Porchlight answers unknown calls for your aging parent, catches scams live, verifies family with one tap, and turns a lamp red or green.">` and `<meta name="theme-color" content="#f5c26b">`.

    Do NOT import from `@/lib/firebase` anywhere in these files — Firebase stays behind the dynamic import in WaitlistForm/LiveCounter.
  </action>
  <acceptance_criteria>
    - `grep -c 'Porchlight' apps/web/src/pages/Landing.tsx` >= 1 and `grep -c 'guardian' apps/web/src/pages/Landing.tsx` >= 1
    - `grep -c 'IC3' apps/web/src/pages/Landing.tsx` >= 1 and `grep -c 'verify' apps/web/src/pages/Landing.tsx` >= 1 and `grep -c 'TODO(human)' apps/web/src/pages/Landing.tsx` == 1
    - `grep -c 'Screens\|Verifies family\|Lights the lamp' apps/web/src/pages/Landing.tsx` >= 3
    - `grep -c '<WaitlistForm' apps/web/src/pages/Landing.tsx` == 1 and `grep -c '<LiveCounter' apps/web/src/pages/Landing.tsx` == 1
    - `grep -c 'Built in 24h at RI Startup Week' apps/web/src/pages/Landing.tsx` == 1
    - `grep -c 'id="waitlist"' apps/web/src/pages/Landing.tsx` == 1
    - `grep -c '<svg' apps/web/src/components/Lamp.tsx` == 1 and `grep -c 'radialGradient' apps/web/src/components/Lamp.tsx` >= 1
    - `grep -c 'porch-glow' apps/web/src/index.css` >= 2 (keyframes + usage) and `grep -c 'prefers-reduced-motion' apps/web/src/index.css` >= 1
    - `grep -c 'name="description"' apps/web/index.html` == 1
    - `grep -c "lib/firebase" apps/web/src/pages/Landing.tsx apps/web/src/components/Lamp.tsx` == 0 for each file
    - `grep -c 'md:' apps/web/src/pages/Landing.tsx` >= 3 (responsive breakpoints present)
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @porchlight/web typecheck && test "$(grep -c 'Built in 24h at RI Startup Week' apps/web/src/pages/Landing.tsx)" = "1" && test "$(grep -c 'IC3' apps/web/src/pages/Landing.tsx)" -ge 1 && test "$(grep -c '<WaitlistForm' apps/web/src/pages/Landing.tsx)" = "1" && test "$(grep -c 'radialGradient' apps/web/src/components/Lamp.tsx)" -ge 1 && test "$(grep -c 'lib/firebase' apps/web/src/pages/Landing.tsx)" = "0"</automated>
    <fails_when>typecheck fails; any locked D-09 string is missing; the form/counter are not reused; the lamp has no gradient glow; Landing statically imports the firebase module</fails_when>
  </verify>
  <done>Landing.tsx contains every D-09 element in mobile-first layout with an SVG amber lamp; Firebase remains lazy.</done>
</task>

<task type="auto">
  <name>Task 2: Route switch for `/`, `/app`, `/stage` placeholders</name>
  <files>apps/web/src/App.tsx, apps/web/src/pages/AppPlaceholder.tsx, apps/web/src/pages/Stage.tsx</files>
  <read_first>
    - .planning/phases/01-foundation-and-landing-page-live/01-CONTEXT.md (D-08 route reservations)
    - apps/web/src/App.tsx (Plan 01 minimal view — replace)
    - apps/web/src/lib/schema.ts (Plan 01 barrel — Stage placeholder imports CALL_STATES from it)
    - firebase.json (SPA rewrite already routes all paths to index.html)
  </read_first>
  <action>
    Replace `App.tsx` with a dependency-free pathname switch (no router library — keeps JS light; Phase 4 can add one if the dashboard needs nested routes): read `window.location.pathname`, strip a trailing slash; if it starts with `/app` render `AppPlaceholder`, if it equals `/stage` render `Stage`, otherwise render `Landing`. Keep `captureRef()` in `main.tsx` untouched so `?ref=` still works on every route.

    `pages/AppPlaceholder.tsx`: heading "Family dashboard — coming in Phase 4", one line "This is where you'll see every screened call, transcript, and risk reason.", link back to `/`. `pages/Stage.tsx`: full-viewport dark warm background (`bg-[oklch(0.2_0.03_60)]`), centered `<Lamp />` at `w-72 md:w-96`, heading "Porchlight — stage view", and a small muted row listing the call states from `CALL_STATES` (imported from `@/lib/schema`) as `Badge`s so the projector view has a real import of the shared schema from day one. No Firebase on either placeholder.
  </action>
  <acceptance_criteria>
    - `grep -c 'window.location.pathname' apps/web/src/App.tsx` == 1
    - `grep -c "'/app'\|\"/app\"\|startsWith('/app')" apps/web/src/App.tsx` >= 1 and `grep -c "/stage" apps/web/src/App.tsx` >= 1
    - `grep -c 'react-router' apps/web/package.json` == 0
    - `grep -c 'CALL_STATES' apps/web/src/pages/Stage.tsx` >= 1 and `grep -c '<Lamp' apps/web/src/pages/Stage.tsx` == 1
    - `grep -c 'Phase 4' apps/web/src/pages/AppPlaceholder.tsx` >= 1
    - `grep -c 'lib/firebase' apps/web/src/App.tsx apps/web/src/pages/AppPlaceholder.tsx apps/web/src/pages/Stage.tsx` == 0 for each file
    - `pnpm --filter @porchlight/web build` exits 0 and `ls apps/web/dist/assets | grep -ci firebase` >= 1 (Firebase still a separate lazy chunk)
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @porchlight/web typecheck && pnpm --filter @porchlight/web build && test "$(grep -c 'window.location.pathname' apps/web/src/App.tsx)" = "1" && test "$(grep -c 'CALL_STATES' apps/web/src/pages/Stage.tsx)" -ge 1 && test "$(ls apps/web/dist/assets | grep -ci firebase)" -ge 1 && test "$(grep -c 'react-router' apps/web/package.json)" = "0"</automated>
    <fails_when>typecheck/build fails; App.tsx does not switch on pathname; Stage does not import the shared constants; the firebase chunk got merged into the entry (a static import crept in); a router dependency was added</fails_when>
  </verify>
  <done>`/`, `/app`, `/stage` each render a distinct view from one Vite app; Firebase remains lazy; no router dependency.</done>
</task>

<task type="auto">
  <name>Task 3: README with run, build, deploy, and secrets instructions; phone/desktop readability check</name>
  <files>README.md</files>
  <read_first>
    - .planning/phases/01-foundation-and-landing-page-live/01-CONTEXT.md (D-02, D-03, D-04, D-11 refs list)
    - .planning/phases/01-foundation-and-landing-page-live/01-RESEARCH.md — "Pattern 5" root scripts, "Pattern 6" bootstrap sequence, "Pitfall 2" (no Java), "Verification Commands"
    - package.json (root scripts as actually written in Plan 01)
  </read_first>
  <action>
    Write `README.md` at the repo root (Markdown, ~60-90 lines): project one-liner; layout table (`apps/web`, `functions`, `packages/shared`, `pi/`, `bridge/`); **Prerequisites** (Node 24, pnpm 11, firebase-tools 15 logged in; JDK 21 only if you want the Firestore emulator — not installed tonight); **Run** — `pnpm install`, `pnpm dev:web` (Vite against the live project via `apps/web/.env.local`), `pnpm dev` (needs JDK 21), `firebase emulators:start --only functions` for callable smoke tests; **Build** — `pnpm -r build`; **Deploy** — `pnpm run deploy` (note explicitly: the script is named `deploy` per D-02 but pnpm has a builtin `deploy` subcommand, so always invoke it as `pnpm run deploy`), `pnpm deploy:web` for hosting-only; **Secrets** — Functions read `ANTHROPIC_API_KEY`, `TWILIO_ACCOUNT_SID`, `TWILIO_AUTH_TOKEN`, `ELEVENLABS_API_KEY` via `defineSecret`; set with `firebase functions:secrets:set NAME`; local emulator values in `functions/.secret.local` (gitignored); web public config in `apps/web/.env.local` from `firebase apps:sdkconfig WEB` (gitignored, template `.env.example`); **Waitlist attribution** — `?ref=` values `li`, `reddit`, `fb`, `friends`, `qr`, `judges`, default `direct`; **Verify live** — the curl for `joinWaitlist` with the `{"data":{...}}` envelope and the `stats/waitlist` REST read (Plan 03 fills the project id). Copy commands verbatim from RESEARCH; do not invent flags.
  </action>
  <acceptance_criteria>
    - `test -f README.md`
    - `grep -c 'pnpm run deploy' README.md` >= 1 and `grep -c 'pnpm -r build' README.md` >= 1
    - `grep -c 'functions:secrets:set' README.md` >= 1 and `grep -c '.secret.local' README.md` >= 1 and `grep -c '.env.local' README.md` >= 1
    - `grep -c 'ref=' README.md` >= 1 and `grep -c 'judges' README.md` >= 1
    - `grep -c 'joinWaitlist' README.md` >= 1
    - `git grep -Ei 'sk-ant-|AC[0-9a-f]{32}' -- README.md` prints nothing
  </acceptance_criteria>
  <verify>
    <automated>test -f README.md && test "$(grep -c 'pnpm run deploy' README.md)" -ge 1 && test "$(grep -c 'functions:secrets:set' README.md)" -ge 1 && test "$(grep -c 'ref=' README.md)" -ge 1 && test -z "$(git grep -Ei 'sk-ant-|AC[0-9a-f]{32}' -- README.md)"</automated>
    <fails_when>README missing; any of run/deploy/secrets/ref sections absent; a real key pattern appears</fails_when>
    <human-check>Open `pnpm dev:web` in a browser at 390px width (phone) and 1280px (desktop): headline, problem, three steps, lamp, form, counter, footer all visible without horizontal scroll; the whole pitch reads in about 20 seconds; the lamp glow is amber and breathes slowly.</human-check>
  </verify>
  <done>README documents run/build/deploy/secrets/refs with the exact commands from RESEARCH; landing page confirmed readable on phone and desktop.</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces (Plan 02)

| Path | Exported symbols / purpose |
|------|----------------------------|
| `apps/web/src/pages/Landing.tsx` | `Landing` (default) — Hero, Problem, HowItWorks, Waitlist, Footer |
| `apps/web/src/components/Lamp.tsx` | `Lamp({ color? })` — inline SVG amber glow, reusable for `/stage` |
| `apps/web/src/pages/AppPlaceholder.tsx` | `AppPlaceholder` (default) — Phase 4 reservation |
| `apps/web/src/pages/Stage.tsx` | `Stage` (default) — projector view placeholder, imports `CALL_STATES` |
| `apps/web/src/App.tsx` | pathname switch `/`, `/app*`, `/stage` |
| `apps/web/src/index.css` | `porch-glow` keyframes + `.lamp-glow` |
| `README.md` | run / build / deploy / secrets / refs |
</artifacts>

<firebase_surfaces>
## Firebase surfaces used / not used

**Used (unchanged from Plan 01):** Hosting SPA rewrite makes `/app` and `/stage` deep links resolve; the page still reaches Functions `onCall` and Firestore `onSnapshot` only through the lazy `@/lib/firebase` module.
**Not used:** Auth, App Check, Analytics, Remote Config — no page content depends on them; Analytics is explicitly deferred (CONTEXT Deferred Ideas).
</firebase_surfaces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public page content | Anything rendered is read by strangers; must not leak internal data |
| README | Committed docs must never carry real credentials |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-06 | Information disclosure | README / page copy | medium | mitigate | Only command names and env var *names* documented; `git grep` for key patterns in Task 3 acceptance; `.env.local`/`.secret.local` referenced by path only. |
| T-01-07 | Tampering (XSS) | Landing copy + Lamp SVG | low | mitigate | All copy is static JSX; the only user text (`protecting`) is handled in Plan 01's form and never re-rendered. |
| T-01-01 | Tampering / DoS | `joinWaitlist` spam (inherited) | medium | accept (this plan) | Mitigation lives in Plan 01 (zod caps, `maxInstances`); rate limiting accepted-not-mitigated tonight per D-10. |
| T-01-SC | Tampering | npm installs | low | mitigate | This plan adds NO new packages (no router); pins unchanged. |
</threat_model>

<verification>
- `pnpm --filter @porchlight/web build` exits 0; firebase chunk still separate (LAND-01 + light JS)
- Every D-09 string present in `Landing.tsx` (grep gates above)
- `/app` and `/stage` render placeholders; `Stage` imports `CALL_STATES`
- README documents `pnpm run deploy`, secrets, and refs
- Human check: readable on phone + desktop
</verification>

<success_criteria>
- All Task 1-3 acceptance criteria pass
- No new npm dependencies; no static Firebase import outside `lib/firebase.ts`
- The page carries the "verify" marker on the IC3 figure until a human removes it
</success_criteria>

<output>
Create `.planning/phases/01-foundation-and-landing-page-live/01-02-SUMMARY.md` when done
</output>
