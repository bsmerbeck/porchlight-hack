---
phase: 01-foundation-and-landing-page-live
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - .gitignore
  - firebase.json
  - firestore.rules
  - firestore.indexes.json
  - packages/shared/package.json
  - packages/shared/tsconfig.json
  - packages/shared/src/index.ts
  - packages/shared/src/waitlist.ts
  - packages/shared/src/calls.ts
  - packages/shared/src/households.ts
  - functions/package.json
  - functions/tsconfig.json
  - functions/src/index.ts
  - apps/web/package.json
  - apps/web/index.html
  - apps/web/vite.config.ts
  - apps/web/tsconfig.json
  - apps/web/tsconfig.app.json
  - apps/web/components.json
  - apps/web/.env.example
  - apps/web/src/main.tsx
  - apps/web/src/App.tsx
  - apps/web/src/index.css
  - apps/web/src/lib/utils.ts
  - apps/web/src/lib/firebase.ts
  - apps/web/src/lib/ref.ts
  - apps/web/src/lib/schema.ts
  - apps/web/src/components/WaitlistForm.tsx
  - apps/web/src/components/LiveCounter.tsx
  - apps/web/src/components/ui/button.tsx
  - apps/web/src/components/ui/input.tsx
  - apps/web/src/components/ui/card.tsx
  - apps/web/src/components/ui/badge.tsx
  - apps/web/src/components/ui/label.tsx
  - pi/README.md
  - bridge/README.md
autonomous: true
requirements: [FND-01, FND-02, FND-03, LAND-02, LAND-03, LAND-04]

must_haves:
  truths:
    - "From a clean checkout, `pnpm install` then `pnpm -r build` exit 0 and produce `apps/web/dist/index.html` and `functions/lib/index.js` (FND-01, D-02)"
    - "`functions/lib/index.js` loads under Node and exports `joinWaitlist`, `onWaitlistCreated`, and `healthz`; the shared zod schema and `CALL_STATES` are inlined in the bundle, and no `@porchlight/shared` require remains (FND-03, D-07)"
    - "Both `apps/web` and `functions` import from `@porchlight/shared` and typecheck against the single `calls/{id}` definition (FND-03, D-05)"
    - "The `/` route renders a waitlist form (email + optional protecting) and a live counter that lazy-loads Firebase and listens to `stats/waitlist` via onSnapshot (LAND-02, LAND-03, D-10)"
    - "`?ref=` is captured to sessionStorage on load, defaults to `direct`, and is sent with every signup (LAND-04, D-11)"
    - "All four vendor secrets are declared with `defineSecret` and bound to a function; `git ls-files` shows no `.env.local` or `.secret.local` (FND-02, D-04)"
  artifacts:
    - "package.json (root, scripts dev/dev:web/build/typecheck/deploy/deploy:web)"
    - "pnpm-workspace.yaml (packages apps/*, packages/*, functions; overrides zod; allowBuilds @firebase/util, esbuild, protobufjs)"
    - "packages/shared/src/{index,calls,households,waitlist}.ts"
    - "functions/src/index.ts -> functions/lib/index.js (esbuild bundle, shared inlined)"
    - "firebase.json, firestore.rules, firestore.indexes.json"
    - "apps/web/src/{main,App}.tsx, lib/{firebase,ref,schema,utils}.ts, components/{WaitlistForm,LiveCounter}.tsx, components/ui/*"
    - "apps/web/.env.example, pi/README.md, bridge/README.md"
  key_links:
    - "functions esbuild `--alias:@porchlight/shared=../packages/shared/src/index.ts` — without it the deployed bundle crashes at cold start on a missing module (RESEARCH Pitfall 3)"
    - "`onWaitlistCreated` -> `stats/waitlist` `{count: increment(1)}` `{merge:true}` — the only writer of the number the client counter listens to"
    - "`apps/web/src/lib/firebase.ts` is only reached via dynamic `import()` from WaitlistForm/LiveCounter — keeps first paint under ~90 kB gz (RESEARCH Pattern 4)"
    - "`firestore.rules` `match /stats/waitlist { allow get: if true }` — the single client-readable doc; everything else closed"
---

<objective>
Scaffold the Porchlight pnpm monorepo and wire the thinnest end-to-end waitlist path: shared zod schema -> `joinWaitlist` callable -> `waitlist/{id}` -> `onWaitlistCreated` trigger -> `stats/waitlist.count` -> `onSnapshot` counter on the `/` route. Declare all four vendor secrets via `defineSecret`. Define the shared `calls/{id}` schema once and import it from both web and functions.

Purpose: Everything later in the hackathon (telephony, scoring, lamp, dashboard) builds on this workspace shape, this deploy command, and this shared schema. Getting the one-command build proven now means Plan 03 is a pure deploy step.
Output: A repo that builds with `pnpm -r build` from a clean checkout, a functions bundle that loads under Node and exposes the three functions, and a minimal `/` route with a working (post-deploy) waitlist form + live counter.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-landing-page-live/01-CONTEXT.md
@.planning/phases/01-foundation-and-landing-page-live/01-RESEARCH.md

**Operating notes for the executor (from 01-RESEARCH.md, all spike-verified):**
- Pins are exact. TypeScript is **6.0.3** everywhere (npm `latest` is 7.x — never install unpinned). `vite@8.1.3`, `@vitejs/plugin-react@6.0.3`, `react@19.2.7`, `react-dom@19.2.7`, `tailwindcss@4.3.2`, `@tailwindcss/vite@4.3.2`, `firebase@12.15.0`, `firebase-functions@7.4.0`, `firebase-admin@14.4.0`, `zod@4.4.3`, `esbuild@0.28.2`, `concurrently@10.0.5`, `@types/node@24.13.2`, `@types/react@19.2.17`, `@types/react-dom@19.2.3`.
- pnpm 11 hard-fails on ignored build scripts: `allowBuilds` for `@firebase/util`, `esbuild`, `protobufjs` MUST be in `pnpm-workspace.yaml` before the first `pnpm install` (Pitfall 1).
- No JDK on this Mac: do not attempt to start the Firestore emulator. Build-level verification only in this plan; live verification is Plan 03.
- `pnpm deploy` collides with a pnpm builtin subcommand; the root script is still named `deploy` per D-02 and is invoked as `pnpm run deploy`.
- Node prints an `Unsupported engine` warning for `functions` (`engines.node "22"` under Node 24). That is a warning, not a failure (Pitfall 4).
- Package legitimacy: every package in the RESEARCH.md audit table is Approved (all `SUS` verdicts are `too-new`-latest only, pinned to spike-verified versions). No human checkpoint needed for installs.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end waitlist write path — workspace root + shared schema + functions bundle + Firestore rules (one path only)</name>
  <files>package.json, pnpm-workspace.yaml, .gitignore, packages/shared/package.json, packages/shared/tsconfig.json, packages/shared/src/index.ts, packages/shared/src/waitlist.ts, functions/package.json, functions/tsconfig.json, functions/src/index.ts, firebase.json, firestore.rules, firestore.indexes.json</files>
  <read_first>
    - .planning/phases/01-foundation-and-landing-page-live/01-CONTEXT.md (D-01, D-02, D-04, D-06, D-07, D-10)
    - .planning/phases/01-foundation-and-landing-page-live/01-RESEARCH.md — "Standard Stack" install block, "Pattern 1", "Pattern 2" (functions/package.json + tsconfig verbatim), "Pattern 3", "Pattern 5" (firebase.json, root package.json scripts, pnpm-workspace.yaml verbatim), "Code Examples" (waitlist.ts, functions/src/index.ts, firestore.rules verbatim), Pitfalls 1, 3, 4, 9
    - .gitignore (existing; extend, do not replace)
  </read_first>
  <action>
    Wire ONE path: a `{email, protecting?, ref}` payload validated by the shared zod schema, written by the `joinWaitlist` callable to `waitlist/{autoId}`, incremented by `onWaitlistCreated` into `stats/waitlist`, readable by anonymous clients through rules. Nothing else on this path yet (the web caller comes in Task 2).

    1. Root `package.json` per D-01/D-02 and RESEARCH Pattern 5: `name` porchlight, `private` true, `type` module, `packageManager` `pnpm@11.9.0`, `engines.node` `>=24`, scripts `dev` (concurrently: `pnpm --filter @porchlight/web dev` + `firebase emulators:start --only functions,firestore,hosting`), `dev:web`, `build` (`pnpm -r build`), `typecheck` (`pnpm -r typecheck`), `deploy` (`pnpm -r build && firebase deploy --only hosting,functions,firestore:rules`), `deploy:web`. Root devDependencies: `typescript@6.0.3`, `concurrently@10.0.5`.
    2. `pnpm-workspace.yaml` verbatim from RESEARCH Pattern 5: packages `apps/*`, `packages/*`, `functions`; `overrides` zod `4.4.3`; `allowBuilds` for `@firebase/util`, `esbuild`, `protobufjs`.
    3. Extend `.gitignore` (Pitfall 9): add `functions/.secret.local`, `functions/lib/`, `*.tsbuildinfo`, `*-debug.log`, `.firebase/`. Keep the existing `.env`, `.env.*`, `!.env.example`, `node_modules/`, `dist/` lines. Do NOT ignore `.firebaserc` (D-03 says commit it).
    4. `packages/shared/package.json` verbatim from RESEARCH Pattern 1 (`@porchlight/shared`, `exports` `.` -> `./src/index.ts`, `main`/`types` same, dependency `zod` `4.4.3`, script `typecheck`: `tsc -p tsconfig.json --noEmit`). `packages/shared/tsconfig.json`: `target` ES2022, `module` ESNext, `moduleResolution` Bundler, `strict`, `skipLibCheck`, `noEmit`, `include` `src`. `src/waitlist.ts` verbatim from RESEARCH Code Examples: `REFS` const tuple, `WaitlistPayload` zod object (email trim/lowercase/email/max 254; protecting trim/max 200/optional; ref trim/max 32/default `direct`), `WaitlistPayload` type, `WaitlistDoc` (adds `createdAt: unknown`, `ua?: string`), `StatsWaitlistDoc` `{count: number}`. `src/index.ts` re-exports `./waitlist` (Task 3 adds `./calls` and `./households`).
    5. `functions/package.json` and `functions/tsconfig.json` verbatim from RESEARCH Pattern 2 — `main` `lib/index.js`, `engines.node` `"22"`, `build` script runs `tsc -p tsconfig.json` then `esbuild src/index.ts --bundle --platform=node --target=node22 --format=cjs --packages=external --alias:@porchlight/shared=../packages/shared/src/index.ts --outfile=lib/index.js`; deps `firebase-admin 14.4.0`, `firebase-functions 7.4.0`, `zod 4.4.3`; devDeps `esbuild 0.28.2`, `typescript 6.0.3`, `@types/node 24.13.2`. Dependencies MUST be plain registry semver only — the shared package is reached through `paths` (tsc) and `--alias` (esbuild), never a package dependency (remote `npm install` cannot resolve the pnpm workspace protocol; RESEARCH Anti-Patterns). Do not add the deprecated base-URL compiler option to any tsconfig (TS 6.0.3 emits TS5101; Pitfall 5).
    6. `functions/src/index.ts` from RESEARCH Code Examples §functions, extended per D-04: imports `onCall`, `HttpsError` from `firebase-functions/https`, `onRequest` from `firebase-functions/https`, `onDocumentCreated` from `firebase-functions/firestore`, `defineSecret` from `firebase-functions/params`, `initializeApp` from `firebase-admin/app`, `getFirestore`, `FieldValue` from `firebase-admin/firestore`, `WaitlistPayload` from `@porchlight/shared`. Call `initializeApp()` once at module scope; `REGION = 'us-central1'`. Declare four secrets with `defineSecret`: `ANTHROPIC_API_KEY`, `TWILIO_ACCOUNT_SID`, `TWILIO_AUTH_TOKEN`, `ELEVENLABS_API_KEY` (exported consts `anthropicKey`, `twilioAccountSid`, `twilioAuthToken`, `elevenLabsKey`). Export `joinWaitlist = onCall({ region, cors: true, maxInstances: 5 }, handler)` — `WaitlistPayload.safeParse(req.data)`, on failure throw `HttpsError('invalid-argument', 'Invalid signup', parsed.error.flatten())`; `ua = req.rawRequest.get('user-agent')?.slice(0, 200)`; `collection('waitlist').add({...parsed.data, ua, createdAt: FieldValue.serverTimestamp()})`; return `{ id }`. Export `onWaitlistCreated = onDocumentCreated({ document: 'waitlist/{id}', region }, ...)` doing `doc('stats/waitlist').set({ count: FieldValue.increment(1) }, { merge: true })`. Export `healthz = onRequest({ region, secrets: [anthropicKey, twilioAccountSid, twilioAuthToken, elevenLabsKey] }, (req, res) => res.json({ ok: true, secrets: [...four names...].map(n => process.env[n] ? 'set' : 'unset') }))` — a placeholder endpoint whose only job is binding the secrets so `functions:secrets:set` + deploy binding are exercised tonight; it reports presence only, never values. Never call `.value()` at module top level (RESEARCH Anti-Patterns).
    7. `firebase.json` verbatim from RESEARCH Pattern 5 (hosting public `apps/web/dist`, ignore list, SPA rewrite `**` -> `/index.html`, immutable cache header on `/assets/**`; functions source `functions`, runtime `nodejs22`, ignore `node_modules`, `.git`, `**/.*`, `src`, `tsconfig.json`; firestore rules/indexes; emulators ports 5001/8080/5000, ui enabled). Add `"predeploy": ["pnpm --filter @porchlight/functions build"]` under `functions` so `firebase deploy --only functions` alone also rebuilds. `firestore.rules` verbatim from RESEARCH Code Examples (rules_version 2; `match /stats/waitlist { allow get: if true; }`; `match /{document=**} { allow read, write: if false; }`). `firestore.indexes.json` = `{"indexes":[],"fieldOverrides":[]}`.
    8. Run `pnpm install` at the root (expect the engine warning for functions; expect NO `ERR_PNPM_IGNORED_BUILDS`). Run `pnpm --filter @porchlight/functions build`.
  </action>
  <acceptance_criteria>
    - `pnpm install` exits 0 and prints no `ERR_PNPM_IGNORED_BUILDS`
    - `pnpm --filter @porchlight/functions build` exits 0 and `functions/lib/index.js` exists
    - `node -e "const m=require('./functions/lib/index.js');['joinWaitlist','onWaitlistCreated','healthz'].forEach(k=>{if(!m[k])process.exit(1)});console.log('ok')"` prints `ok`
    - `grep -c 'safeParse' functions/lib/index.js` >= 1 (shared zod schema inlined)
    - `grep -c "require(\"@porchlight/shared\")" functions/lib/index.js` == 0 (alias applied, nothing left external)
    - `grep -c 'workspace:' functions/package.json` == 0
    - `grep -c '"baseUrl"' functions/tsconfig.json packages/shared/tsconfig.json` == 0 for each file
    - `grep -c 'ANTHROPIC_API_KEY\|TWILIO_ACCOUNT_SID\|TWILIO_AUTH_TOKEN\|ELEVENLABS_API_KEY' functions/src/index.ts` >= 4
    - `grep -c 'allow get: if true' firestore.rules` == 1 and `grep -c 'allow read, write: if false' firestore.rules` == 1
    - `grep -c 'allowBuilds' pnpm-workspace.yaml` == 1
    - `.gitignore` contains lines `functions/.secret.local` and `functions/lib/` and does NOT contain `.firebaserc`
  </acceptance_criteria>
  <verify>
    <automated>pnpm install && pnpm --filter @porchlight/functions build && node -e "const m=require('./functions/lib/index.js');['joinWaitlist','onWaitlistCreated','healthz'].forEach(k=>{if(!m[k])process.exit(1)});if(m.joinWaitlist.__endpoint.platform!=='gcfv2')process.exit(2);console.log('ok')" && test "$(grep -c 'require("@porchlight/shared")' functions/lib/index.js)" = "0" && test "$(grep -c 'workspace:' functions/package.json)" = "0"</automated>
    <fails_when>install prints ERR_PNPM_IGNORED_BUILDS; tsc reports TS5101 or any error; node exits non-zero (missing export or not gcfv2); the bundle still requires the shared package by name; functions/package.json has a workspace specifier</fails_when>
  </verify>
  <done>Workspace installs, functions bundle builds and loads with joinWaitlist/onWaitlistCreated/healthz exported, shared zod schema inlined, rules closed except `stats/waitlist` get, all four secrets declared and bound.</done>
</task>

<task type="auto">
  <name>Task 2: Web app scaffold with the working waitlist form + live counter on `/`</name>
  <files>apps/web/package.json, apps/web/index.html, apps/web/vite.config.ts, apps/web/tsconfig.json, apps/web/tsconfig.app.json, apps/web/components.json, apps/web/.env.example, apps/web/src/main.tsx, apps/web/src/App.tsx, apps/web/src/index.css, apps/web/src/lib/utils.ts, apps/web/src/lib/firebase.ts, apps/web/src/lib/ref.ts, apps/web/src/components/WaitlistForm.tsx, apps/web/src/components/LiveCounter.tsx, apps/web/src/components/ui/button.tsx, apps/web/src/components/ui/input.tsx, apps/web/src/components/ui/card.tsx, apps/web/src/components/ui/badge.tsx, apps/web/src/components/ui/label.tsx</files>
  <read_first>
    - .planning/phases/01-foundation-and-landing-page-live/01-CONTEXT.md (D-01, D-04, D-08, D-10, D-11; "Specific Ideas": keep JS light, counter must tick)
    - .planning/phases/01-foundation-and-landing-page-live/01-RESEARCH.md — "Standard Stack" install block (apps/web lines + shadcn init/add lines), "Pattern 4" (lazy Firebase), "Pitfall 5" (shadcn init order + preset + amber theme vars), "Pitfall 6", "Pitfall 10", "Code Examples" (lib/firebase.ts, lib/ref.ts, LiveCounter, .env.example verbatim)
    - packages/shared/src/waitlist.ts (from Task 1 — the schema the form validates against)
  </read_first>
  <action>
    Build the browser end of the Task 1 path: a form that calls `joinWaitlist` with `{email, protecting?, ref}` and a counter that listens to `stats/waitlist`. After this task a visitor on `/` can sign up and watch the number tick (once deployed in Plan 03).

    1. `apps/web/package.json`: name `@porchlight/web`, private, `type` module, scripts `dev` (`vite`), `build` (`vite build`), `typecheck` (`tsc -p tsconfig.app.json --noEmit`), `preview`. Install with the exact RESEARCH commands: `pnpm --filter @porchlight/web add react@19.2.7 react-dom@19.2.7 firebase@12.15.0 zod@4.4.3 '@porchlight/shared@workspace:*'` and `pnpm --filter @porchlight/web add -D vite@8.1.3 @vitejs/plugin-react@6.0.3 tailwindcss@4.3.2 @tailwindcss/vite@4.3.2 typescript@6.0.3 @types/node@24.13.2 @types/react@19.2.17 @types/react-dom@19.2.3`. (The workspace protocol is fine HERE — only `functions/package.json` must avoid it.)
    2. BEFORE shadcn init (Pitfall 5), create `tsconfig.json` (project references to `tsconfig.app.json`, plus `compilerOptions.paths` `@/*` -> `./src/*` so the shadcn CLI can read the alias) and `tsconfig.app.json` (`target` ES2022, `module` ESNext, `moduleResolution` Bundler, `jsx` react-jsx, `strict`, `skipLibCheck`, `noEmit`, `types` `["vite/client"]`, `paths` `@/*` -> `./src/*`, `include` `src` and `../../packages/shared/src`). Do not add the deprecated base-URL compiler option (TS5101). Create `vite.config.ts` with `react()` and `tailwindcss()` plugins and `resolve.alias` `@` -> `fileURLToPath(new URL('./src', import.meta.url))`. Create `index.html` (title `Porchlight`, `<meta name="viewport" content="width=device-width, initial-scale=1">`, `<div id="root">`, module script `/src/main.tsx`), `src/index.css` containing `@import "tailwindcss";`, and a trivial `src/main.tsx`.
    3. From `apps/web`: `pnpm dlx shadcn@4.21.0 init -y --base radix --preset nova` (the preset name is `nova`, NOT `base-nova`), then `pnpm dlx shadcn@4.21.0 add -y button input card badge label`. This writes `components.json`, `src/lib/utils.ts`, rewrites `src/index.css`, and adds runtime deps (`shadcn`, `cn`, `radix-ui`, `lucide-react`, `class-variance-authority`, `tw-animate-css`, `@fontsource-variable/geist`). Then in `src/index.css`: remove the `@import "@fontsource-variable/geist";` line and `pnpm --filter @porchlight/web remove @fontsource-variable/geist` (saves ~76 kB of woff2 for the bad-Wi-Fi room; system font stack is fine — Claude's discretion). Override `:root` theme vars for a warm porchlight palette, e.g. `--background: oklch(0.99 0.01 85)`, `--foreground: oklch(0.25 0.03 60)`, `--primary: oklch(0.78 0.16 70)`, `--primary-foreground: oklch(0.2 0.03 60)`, `--accent: oklch(0.95 0.05 80)`; keep the `@theme inline` block shadcn generated.
    4. `src/lib/ref.ts` verbatim from RESEARCH (D-11): key `porchlight:ref`; `captureRef()` reads `?ref=` from `window.location.search`, stores it (sliced to 32 chars) in `sessionStorage`, returns stored value or `direct`. Call `captureRef()` once in `main.tsx` before `createRoot(...).render(...)`.
    5. `src/lib/firebase.ts` verbatim from RESEARCH Code Examples: `initializeApp` with `VITE_FIREBASE_API_KEY`, `VITE_FIREBASE_AUTH_DOMAIN`, `VITE_FIREBASE_PROJECT_ID`, `VITE_FIREBASE_APP_ID`; export `db`, `fns = getFunctions(app, 'us-central1')`, `joinWaitlist = httpsCallable<WaitlistPayload, {id: string}>(fns, 'joinWaitlist')`, `watchWaitlistCount(cb)` using `onSnapshot(doc(db, 'stats', 'waitlist'))` with `?? 0` default. Include the commented emulator-wiring block gated on `VITE_USE_EMULATORS === '1'`. This module MUST only be reached via dynamic `import('@/lib/firebase')` — never a static import from any component (Pattern 4; first paint stays ~90 kB gz).
    6. `src/components/LiveCounter.tsx` verbatim from RESEARCH (lazy `import('@/lib/firebase')` in `useEffect`, unsubscribe on unmount, renders `{n ?? '…'} families on the waitlist` with `tabular-nums`). `src/components/WaitlistForm.tsx`: shadcn `Card`, `Label`, `Input`, `Button`; controlled `email` and `protecting` fields; on submit run `WaitlistPayload.safeParse({ email, protecting: protecting || undefined, ref: sessionStorage.getItem('porchlight:ref') ?? 'direct' })` client-side for instant feedback (server re-validates — trust boundary is the function), then `const { joinWaitlist } = await import('@/lib/firebase'); await joinWaitlist(parsed.data)`. States: idle / submitting (button disabled) / success ("You're on the list — we'll email you when Porchlight is ready for your family.") / error (show a calm "Something went wrong — try again in a minute." and keep the values). No `dangerouslySetInnerHTML` anywhere.
    7. `src/App.tsx`: render a minimal `/` view for now — an `h1` `Porchlight`, `<WaitlistForm />`, `<LiveCounter />` — Plan 02 replaces this with the full landing page and routes. `.env.example` verbatim from RESEARCH (four `VITE_FIREBASE_*` keys; `VITE_FIREBASE_AUTH_DOMAIN` and `VITE_FIREBASE_PROJECT_ID` left blank until Plan 03 fills `.env.local`). Only `VITE_FIREBASE_*` keys may ever appear in `apps/web` (Pitfall 10).
    8. `pnpm --filter @porchlight/web build` and confirm the Vite output lists a separate `firebase-*.js` (or similarly named) lazy chunk and the entry chunk is not the ~700 kB single-chunk shape.
  </action>
  <acceptance_criteria>
    - `pnpm --filter @porchlight/web build` exits 0 and `apps/web/dist/index.html` exists
    - `pnpm --filter @porchlight/web typecheck` exits 0
    - `ls apps/web/dist/assets | grep -c -i firebase` >= 1 (Firebase is a separate lazy chunk)
    - `grep -rc "from '@/lib/firebase'" apps/web/src --include=*.tsx | grep -v ':0'` prints nothing (no static import of the firebase module)
    - `grep -c "import('@/lib/firebase')" apps/web/src/components/WaitlistForm.tsx apps/web/src/components/LiveCounter.tsx` == 1 for each file
    - `grep -c 'porchlight:ref' apps/web/src/lib/ref.ts` >= 1 and `grep -c "'direct'" apps/web/src/lib/ref.ts` >= 1
    - `grep -c 'captureRef()' apps/web/src/main.tsx` == 1
    - `grep -c 'httpsCallable' apps/web/src/lib/firebase.ts` >= 1 and `grep -c 'onSnapshot' apps/web/src/lib/firebase.ts` >= 1
    - `grep -c '"baseUrl"' apps/web/tsconfig.json apps/web/tsconfig.app.json` == 0 for each file
    - `grep -c 'VITE_FIREBASE_' apps/web/.env.example` == 4 and `grep -c 'ANTHROPIC\|TWILIO\|ELEVENLABS' apps/web/.env.example` == 0
    - `grep -rc 'dangerouslySetInnerHTML' apps/web/src | grep -v ':0'` prints nothing
    - `ls apps/web/src/components/ui/` lists `button.tsx input.tsx card.tsx badge.tsx label.tsx`
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @porchlight/web typecheck && pnpm --filter @porchlight/web build && test -f apps/web/dist/index.html && test "$(ls apps/web/dist/assets | grep -ci firebase)" -ge 1 && test "$(grep -c '"baseUrl"' apps/web/tsconfig.app.json)" = "0"</automated>
    <fails_when>tsc reports TS5101 or any type error; vite build fails; dist/index.html missing; no separate firebase chunk (firebase was statically imported); the deprecated base-URL option is present</fails_when>
  </verify>
  <done>Web app builds and typechecks; `/` renders WaitlistForm + LiveCounter that lazy-load Firebase; `?ref=` captured to sessionStorage with `direct` default; `.env.example` committed with only `VITE_FIREBASE_*` keys.</done>
</task>

<task type="auto">
  <name>Task 3: Shared `calls/{id}` + `households` schema defined once and imported by both web and functions</name>
  <files>packages/shared/src/calls.ts, packages/shared/src/households.ts, packages/shared/src/index.ts, functions/src/index.ts, apps/web/src/lib/schema.ts</files>
  <read_first>
    - .planning/phases/01-foundation-and-landing-page-live/01-CONTEXT.md (D-05, D-06, D-07 — field lists are locked)
    - .planning/phases/01-foundation-and-landing-page-live/01-RESEARCH.md — "Code Examples §packages/shared" (calls.ts verbatim), Assumption A7 (epoch-ms timestamps)
    - packages/shared/src/index.ts, functions/src/index.ts (from Task 1)
  </read_first>
  <action>
    1. `packages/shared/src/calls.ts` verbatim from RESEARCH Code Examples: `CALL_STATES` const tuple `['idle','screening','verifying','verified','scam','ended']`, `CallState` type, `CALL_TRANSITIONS: Record<CallState, readonly CallState[]>` (idle->screening; screening->verifying|scam|ended; verifying->verified|scam|ended; verified->ended; scam->ended; ended->[]), `TACTICS` const tuple `['urgency','secrecy','payment_method','authority_bail','impersonation']`, `Tactic` type, `CallTurn`, and `CallDoc` with exactly the D-05 fields: `householdId`, `state`, `from`, `startedAt`, `endedAt?`, `provider: 'elevenlabs' | 'simulator'`, `providerCallId?`, `turns: CallTurn[]`, `risk: { score, tactics: Tactic[], claimedIdentity?, recommendedAction: 'continue'|'verify'|'end', updatedAt }`, `verification?: { memberId, promptedAt, answeredAt?, answer?: 'yes'|'no'|'timeout', method?: 'passkey'|'link' }`, `outcome?: 'verified'|'scam'|'screened'`, `report?: string`. Timestamps are epoch-ms numbers (A7). Also export `RECOMMENDED_ACTIONS` const tuple `['continue','verify','end']` and `PROVIDERS` `['elevenlabs','simulator']` so Phase 2/3 have them as constants per D-07.
    2. `packages/shared/src/households.ts` per D-06: `HouseholdMember { id, name, relation, aliases: string[], passkeyCredentialIds: string[] }`, `HouseholdDoc { name, seniorName, members: HouseholdMember[] }`, and `DEMO_HOUSEHOLD_ID = 'demo'` const (the single demo household; no real senior data per CLAUDE.md).
    3. `packages/shared/src/index.ts`: `export * from './calls'`, `export * from './households'`, `export * from './waitlist'`.
    4. `functions/src/index.ts`: import `CALL_STATES` from `@porchlight/shared` and include `states: CALL_STATES` in the `healthz` JSON response (proves the constant is inlined into the bundle and gives Phase 2 a smoke endpoint). Rebuild functions.
    5. `apps/web/src/lib/schema.ts`: a tiny barrel the Phase 4 dashboard and `/stage` view will import: `export { CALL_STATES, CALL_TRANSITIONS, TACTICS, REFS, WaitlistPayload } from '@porchlight/shared'` and `export type { CallDoc, CallState, CallTurn, Tactic, HouseholdDoc, HouseholdMember, StatsWaitlistDoc } from '@porchlight/shared'`. Import `REFS` from `@/lib/schema` in `WaitlistForm.tsx` is NOT required; leave the form importing `WaitlistPayload` from `@porchlight/shared` directly — the barrel just has to typecheck as part of the web build (both consumers now compile against the single definition, FND-03).
  </action>
  <acceptance_criteria>
    - `grep -c "export const CALL_STATES" packages/shared/src/calls.ts` == 1 and `grep -c 'CALL_TRANSITIONS' packages/shared/src/calls.ts` >= 1 and `grep -c 'TACTICS' packages/shared/src/calls.ts` >= 1
    - `grep -c 'passkeyCredentialIds' packages/shared/src/households.ts` == 1
    - `grep -c "./calls\|./households\|./waitlist" packages/shared/src/index.ts` == 3
    - `grep -c 'CALL_STATES' functions/src/index.ts` >= 1
    - `pnpm --filter @porchlight/functions build` exits 0 and `grep -c '"screening"' functions/lib/index.js` >= 1 (constant inlined)
    - `grep -c '@porchlight/shared' apps/web/src/lib/schema.ts` == 2 and `pnpm --filter @porchlight/web typecheck` exits 0
    - `grep -l '@porchlight/shared' apps/web/src/lib/firebase.ts apps/web/src/lib/schema.ts apps/web/src/components/WaitlistForm.tsx functions/src/index.ts` lists all four files
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @porchlight/shared typecheck && pnpm --filter @porchlight/functions build && test "$(grep -c '"screening"' functions/lib/index.js)" -ge 1 && pnpm --filter @porchlight/web typecheck && grep -l '@porchlight/shared' apps/web/src/lib/schema.ts functions/src/index.ts | wc -l | grep -q 2</automated>
    <fails_when>shared/functions/web typecheck fails; `screening` literal absent from the bundle (constant not inlined); either consumer does not import the shared package</fails_when>
  </verify>
  <done>`calls/{id}`, `households/{id}`, `waitlist/{id}`, `stats/waitlist` types and `CALL_STATES`/`CALL_TRANSITIONS`/`TACTICS` constants exist once in `packages/shared` and are compiled into both web and functions.</done>
</task>

<task type="auto">
  <name>Task 4: Placeholder packages, full-tree build, and secrets-not-in-git proof</name>
  <files>pi/README.md, bridge/README.md, .gitignore</files>
  <read_first>
    - .planning/phases/01-foundation-and-landing-page-live/01-CONTEXT.md (D-01 pi/ and bridge/ placeholders; D-04 secrets)
    - .planning/phases/01-foundation-and-landing-page-live/01-RESEARCH.md — "Verification Commands" table (FND-01, FND-02, FND-03 rows), Pitfall 9, Pitfall 10
    - .planning/PROJECT.md (Pi constraints: Python 3.9 stdlib + sense_hat only)
  </read_first>
  <action>
    1. `pi/README.md`: 6-10 lines — Porchlight lamp controller placeholder; Python 3.9, standard library plus preinstalled `sense_hat` only (no `pip install` on the Pi); driven from the Mac over the ethernet cable, never Wi-Fi; filled in Phase 3. `bridge/README.md`: 6-10 lines — Node bridge script placeholder that will watch `calls/{id}` in Firestore and push lamp state to the Pi; imports types from `@porchlight/shared`; filled in Phase 3. Neither directory gets a `package.json` tonight (keeps `pnpm -r` fast and Cloud Build untouched).
    2. Confirm `.gitignore` covers: `node_modules/`, `dist/`, `.env`, `.env.*`, `!.env.example`, `functions/.secret.local`, `functions/lib/`, `.firebase/`, `*.tsbuildinfo`, `*-debug.log`. Create a local `functions/.secret.local` containing only the line `ANTHROPIC_API_KEY=placeholder-until-checkpoint` and an `apps/web/.env.local` copied from `.env.example` — both MUST be ignored by git (this proves the ignore rules before real values arrive in Plan 03).
    3. Run the full-tree proofs from the RESEARCH Verification Commands table: `pnpm -r build`, `pnpm -r typecheck`, the secrets-in-git greps.
  </action>
  <acceptance_criteria>
    - `test -f pi/README.md && test -f bridge/README.md`
    - `pnpm -r build` exits 0; `ls apps/web/dist/index.html functions/lib/index.js` lists both
    - `pnpm -r typecheck` exits 0
    - `git ls-files | grep -Ei '\.env($|\.local)|secret\.local'` prints nothing
    - `git check-ignore -q functions/.secret.local apps/web/.env.local functions/lib/index.js` exits 0
    - `git grep -Ei 'sk-ant-|AC[0-9a-f]{32}' -- ':!*.md'` prints nothing
    - `git ls-files --error-unmatch .gitignore` exits 0 and `git check-ignore -q .firebaserc` exits 1 (it is NOT ignored)
  </acceptance_criteria>
  <verify>
    <automated>pnpm -r build && pnpm -r typecheck && test -f apps/web/dist/index.html && test -f functions/lib/index.js && test -z "$(git ls-files | grep -Ei '\.env($|\.local)|secret\.local')" && git check-ignore -q functions/.secret.local apps/web/.env.local && ! git check-ignore -q .firebaserc</automated>
    <fails_when>any package build/typecheck fails; a `.env.local`/`.secret.local` path is tracked; the ignore rules miss either local secrets file; `.firebaserc` would be ignored (D-03 requires committing it)</fails_when>
  </verify>
  <done>One command (`pnpm -r build`) builds web + functions from a clean checkout; secrets files are provably untracked; `pi/` and `bridge/` exist with READMEs.</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces (Plan 01)

| Path | Exported symbols / purpose |
|------|----------------------------|
| `package.json` | scripts `dev`, `dev:web`, `build`, `typecheck`, `deploy`, `deploy:web` |
| `pnpm-workspace.yaml` | workspace globs, zod override, `allowBuilds` |
| `packages/shared/src/index.ts` | re-exports `./calls`, `./households`, `./waitlist` |
| `packages/shared/src/calls.ts` | `CALL_STATES`, `CallState`, `CALL_TRANSITIONS`, `TACTICS`, `Tactic`, `RECOMMENDED_ACTIONS`, `PROVIDERS`, `CallTurn`, `CallDoc` |
| `packages/shared/src/households.ts` | `HouseholdMember`, `HouseholdDoc`, `DEMO_HOUSEHOLD_ID` |
| `packages/shared/src/waitlist.ts` | `REFS`, `WaitlistPayload` (zod + type), `WaitlistDoc`, `StatsWaitlistDoc` |
| `functions/src/index.ts` -> `functions/lib/index.js` | `joinWaitlist` (onCall), `onWaitlistCreated` (onDocumentCreated), `healthz` (onRequest, binds secrets), `anthropicKey`, `twilioAccountSid`, `twilioAuthToken`, `elevenLabsKey` |
| `firebase.json`, `firestore.rules`, `firestore.indexes.json` | Hosting SPA + Functions nodejs22 + rules (deny-all, `stats/waitlist` get) |
| `apps/web/src/lib/firebase.ts` | `db`, `fns`, `joinWaitlist`, `watchWaitlistCount` (lazy-loaded only) |
| `apps/web/src/lib/ref.ts` | `captureRef` |
| `apps/web/src/lib/schema.ts` | barrel re-export of shared constants/types for Phase 4 |
| `apps/web/src/components/WaitlistForm.tsx`, `LiveCounter.tsx` | `WaitlistForm`, `LiveCounter` |
| `apps/web/src/components/ui/*` | shadcn `Button`, `Input`, `Card`, `Badge`, `Label` |
| `apps/web/.env.example` | `VITE_FIREBASE_API_KEY`, `VITE_FIREBASE_AUTH_DOMAIN`, `VITE_FIREBASE_PROJECT_ID`, `VITE_FIREBASE_APP_ID` |
| `pi/README.md`, `bridge/README.md` | placeholders per D-01 |
</artifacts>

<firebase_surfaces>
## Firebase surfaces used / not used

**Used:** Hosting (static `apps/web/dist`, SPA rewrite); Functions v2 `onCall` (`joinWaitlist`), `onRequest` (`healthz`), `onDocumentCreated` (`onWaitlistCreated`); `defineSecret` (four vendor keys, Secret Manager); Firestore rules (deny-all + `stats/waitlist` get); web SDK `onSnapshot` (counter).
**Not used:** Auth (public landing, no accounts until Phase 4 passkeys); App Check (D-10 accepts zod-only abuse limiting tonight); Analytics (deferred — `?ref=` counts are the only attribution); Remote Config (nothing to toggle).
</firebase_surfaces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> `joinWaitlist` callable | Untrusted JSON from anyone on the internet (curl or the form) |
| Browser -> Firestore (rules) | Anonymous clients; rules are the only gate |
| Repo -> Cloud Build / Hosting | Anything committed or in the web bundle is public |
| Functions runtime -> Secret Manager | Secrets readable only inside bound function handlers |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering / DoS | `joinWaitlist` spam, counter inflation | medium | mitigate (partial) | zod `WaitlistPayload` with hard length caps (254/200/32), email normalization, `maxInstances: 5` caps cost. Per-IP rate limiting and App Check are **accepted-not-mitigated** tonight (D-10) — a padded counter is a cosmetic risk for a demo; reconcile from `waitlist` size if needed (RESEARCH Pitfall 8). |
| T-01-02 | Information disclosure | Secrets in git or web bundle | high | mitigate | Only `VITE_FIREBASE_*` in `apps/web`; vendor keys via `defineSecret` read inside handlers only; `.env.local`/`.secret.local` gitignored and proven by `git ls-files` + `git check-ignore` in Task 4. |
| T-01-03 | Elevation of privilege | Open Firestore rules | high | mitigate | `firestore.rules` denies all client reads/writes except `get` on `stats/waitlist`; Admin SDK in Functions is the only writer. |
| T-01-04 | Tampering (XSS) | `protecting` free text | low | mitigate | React escaping; no `dangerouslySetInnerHTML` (asserted by grep); value never rendered to other visitors. |
| T-01-05 | Spoofing | CORS on public callable | low | accept | `onCall` with `cors: true` is intended for a public signup; no credentials involved. |
| T-01-SC | Tampering | npm installs (`cn@0.3.2`, `firebase-functions@7.4.0` fresh releases) | medium | mitigate | Exact pins from RESEARCH Package Legitimacy Audit (all Approved; `SUS` = too-new only); `allowBuilds` limited to the 3 known postinstalls; no unpinned installs. |
</threat_model>

<verification>
- `pnpm install && pnpm -r build && pnpm -r typecheck` exit 0 from a clean checkout (FND-01)
- `node -e "require('./functions/lib/index.js')"` loads; exports `joinWaitlist`, `onWaitlistCreated`, `healthz`; bundle has no `require("@porchlight/shared")`; `"screening"` literal present (FND-03)
- `git ls-files | grep -Ei '\.env($|\.local)|secret\.local'` empty; `git check-ignore` confirms both local secrets files ignored; `.firebaserc` not ignored (FND-02)
- Web build emits a separate firebase chunk; `captureRef()` called in `main.tsx`; form submits `{email, protecting?, ref}` through the callable (LAND-02/03/04 build-level; live proof in Plan 03)
</verification>

<success_criteria>
- Every acceptance criterion in Tasks 1-4 passes
- All four secrets are declared with `defineSecret` and bound to `healthz`
- The shared schema exists once and is imported by `functions/src/index.ts` and `apps/web/src/lib/{firebase,schema}.ts`
- No `.env.local`, `.secret.local`, `functions/lib`, or `dist` path is tracked
</success_criteria>

<output>
Create `.planning/phases/01-foundation-and-landing-page-live/01-01-SUMMARY.md` when done
</output>
