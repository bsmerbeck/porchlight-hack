---
phase: 01-foundation-and-landing-page-live
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - .firebaserc
autonomous: false
requirements: []
# No new requirement IDs: this plan is the live, end-to-end proof of FND-02 (project + Blaze + secrets),
# LAND-02 (signup lands in Firestore), LAND-03 (counter ticks live), LAND-04 (ref attributed) and the
# `pnpm run deploy` half of FND-01. Those IDs are owned by Plan 01's `requirements` field.
user_setup:
  - service: firebase
    why: "Project, Blaze billing, Firestore database, web app config, and Secret Manager values can only be created by the account owner"
    env_vars:
      - name: VITE_FIREBASE_API_KEY / VITE_FIREBASE_AUTH_DOMAIN / VITE_FIREBASE_PROJECT_ID / VITE_FIREBASE_APP_ID
        source: "`firebase apps:sdkconfig WEB` -> apps/web/.env.local (gitignored)"
      - name: ANTHROPIC_API_KEY (+ placeholder TWILIO_ACCOUNT_SID, TWILIO_AUTH_TOKEN, ELEVENLABS_API_KEY)
        source: "`firebase functions:secrets:set NAME` (Secret Manager; never in git)"
    dashboard_config:
      - task: "Upgrade to Blaze (pay-as-you-go)"
        location: "https://console.firebase.google.com/project/<id>/usage/details"

must_haves:
  truths:
    - "A Firebase project `porchlight-<suffix>` exists on Blaze with a `(default)` Firestore database in `nam5`; `.firebaserc` points at it and is committed (FND-02, D-03)"
    - "`pnpm run deploy` from the repo root ends with `Deploy complete!` and the page is served at `https://<id>.web.app` (FND-01, D-02, D-12)"
    - "`ANTHROPIC_API_KEY` is readable by Functions from Secret Manager; no secret value exists in git (FND-02, D-04)"
    - "A signup posted to `joinWaitlist` appears as `waitlist/{id}` with `email`, `ref`, `createdAt` and the `stats/waitlist.count` increments within seconds (LAND-02, LAND-03, LAND-04, D-10, D-11)"
    - "The counter on the live page ticks without a reload when a second browser signs up (LAND-03)"
    - "The URL has been posted with `?ref=li|reddit|fb|friends` and a QR code encodes `?ref=qr`; the baseline count is recorded"
  artifacts:
    - ".firebaserc (committed)"
    - "apps/web/.env.local (gitignored, real VITE_FIREBASE_* values)"
    - "Secret Manager: ANTHROPIC_API_KEY (real), TWILIO_ACCOUNT_SID / TWILIO_AUTH_TOKEN / ELEVENLABS_API_KEY (placeholders)"
    - "Deployed: Hosting site, functions joinWaitlist / onWaitlistCreated / healthz, firestore rules"
  key_links:
    - "Blaze must be active before `firebase deploy --only functions` — otherwise the CLI stops at cloudbuild enablement (RESEARCH Pattern 6 / Pitfall 7)"
    - "All four secrets bound to `healthz` must exist in Secret Manager or the functions deploy fails — hence placeholder values for the three not yet available"
    - "Eventarc service-agent propagation: the `onWaitlistCreated` trigger may need one retry of `firebase deploy --only functions` after 2-3 minutes"
---

<objective>
Create the Firebase project (human), wire public config and secrets without touching git, run the one-command deploy, and prove the whole waitlist loop live: curl -> Firestore -> trigger -> counter ticks in a browser. Then put the page in front of people with attributed `?ref=` links and a QR code so traction accrues overnight.

Purpose: This is the moment the landing page starts collecting real signups for the traction slide. Everything here is on the critical path and mostly waiting on cloud propagation, so it runs in parallel with Plan 02's content work.
Output: Live URL, committed `.firebaserc`, verified end-to-end signup + counter, links/QR posted, baseline count recorded.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-landing-page-live/01-CONTEXT.md
@.planning/phases/01-foundation-and-landing-page-live/01-RESEARCH.md
@.planning/phases/01-foundation-and-landing-page-live/01-01-SUMMARY.md

**Timing expectations (RESEARCH Pattern 6 / Pitfall 7):** project + Blaze + database ~5 min of human clicks; first 2nd-gen Functions deploy 3-6 min; Eventarc trigger may fail once with `Permission denied while using the Eventarc Service Agent` — wait 2-3 min and re-run `firebase deploy --only functions`. Deploy Hosting first if Functions lag so the URL is public immediately.

**Substitute `<id>` below with the real project id from Task 1** (e.g. `porchlight-ri`). `<id>` is also the value in `.firebaserc`.
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Create the Firebase project, enable Blaze, create Firestore, wire web config and secrets</name>
  <files>.firebaserc</files>
  <read_first>
    - .planning/phases/01-foundation-and-landing-page-live/01-CONTEXT.md (D-03, D-04)
    - .planning/phases/01-foundation-and-landing-page-live/01-RESEARCH.md — "Pattern 6" (exact CLI sequence), "Environment Availability", "Open Questions" #2 (nam5)
    - apps/web/.env.example (Plan 01 — the keys to fill)
  </read_first>
  <reversibility rating="one-way">The Firebase project id and the Firestore database location (`nam5`) cannot be changed after creation; the id also becomes the public hostname `<id>.web.app` (D-12). Pick once.</reversibility>
  <action>
    The human (CLI already logged in as bsmerbeck@gmail.com) runs these in order from the repo root. Claude cannot do steps 1-3 or 6 (billing, account-owned resources, typing a secret). Claude prepares nothing here beyond waiting; do NOT try to automate any step.

    1. `firebase projects:create porchlight-ri --display-name Porchlight` — the id must be globally unique; on collision retry with a suffix (`porchlight-ri2`, `porchlight-pvd`). **This id and the database location in step 3 are permanent** (project id = hostname `<id>.web.app`; Firestore location cannot be moved).
    2. Upgrade to Blaze in the console: `https://console.firebase.google.com/project/<id>/usage/details` -> Modify plan -> Blaze (link the existing billing account). Without this, `firebase deploy --only functions` stops with the "must be on the Blaze plan" error (RESEARCH Pattern 6).
    3. `firebase use <id>` (writes `.firebaserc` — commit it per D-03), then create the Firestore database: `firebase firestore:databases:create "(default)" --location nam5` — if the CLI rejects the `(default)` name, use the console: Build -> Firestore Database -> Create database -> production mode -> location `nam5` (RESEARCH A3).
    4. `firebase apps:create WEB porchlight-web`, then `firebase apps:sdkconfig WEB`. Copy `apiKey`, `authDomain`, `projectId`, `appId` into `apps/web/.env.local` as `VITE_FIREBASE_API_KEY`, `VITE_FIREBASE_AUTH_DOMAIN`, `VITE_FIREBASE_PROJECT_ID`, `VITE_FIREBASE_APP_ID` (file is gitignored; template is `.env.example`).
    5. `firebase functions:secrets:set ANTHROPIC_API_KEY` and paste the real key at the prompt (not on the command line — keeps it out of shell history). Sanity: `firebase functions:secrets:access ANTHROPIC_API_KEY | cut -c1-7` prints `sk-ant-`.
    6. Placeholder values for the three secrets bound to `healthz` but not available until Phase 2 (deploy fails if a bound secret does not exist): `firebase functions:secrets:set TWILIO_ACCOUNT_SID` (type `placeholder`), `firebase functions:secrets:set TWILIO_AUTH_TOKEN` (`placeholder`), `firebase functions:secrets:set ELEVENLABS_API_KEY` (`placeholder`). Phase 2's checkpoint overwrites them.
    7. Optionally write the real Anthropic key into `functions/.secret.local` (`ANTHROPIC_API_KEY=sk-ant-...`) for local emulator use — gitignored (Plan 01 Task 4 proved it).

    Then tell Claude: "project <id> ready".
  </action>
  <acceptance_criteria>
    - `firebase projects:list | grep -c porchlight` >= 1
    - `cat .firebaserc | grep -c porchlight` == 1
    - `firebase apps:list WEB | grep -c porchlight-web` >= 1
    - `grep -c '^VITE_FIREBASE_API_KEY=.\+' apps/web/.env.local` == 1 and `grep -c "^VITE_FIREBASE_PROJECT_ID=<id>" apps/web/.env.local` == 1
    - `firebase functions:secrets:access ANTHROPIC_API_KEY | cut -c1-7` prints `sk-ant-`
    - `firebase functions:secrets:access TWILIO_ACCOUNT_SID | wc -c` > 0 (placeholder exists); same for `TWILIO_AUTH_TOKEN`, `ELEVENLABS_API_KEY`
    - `git status --porcelain | grep -c '.env.local\|.secret.local'` == 0 (still ignored)
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -c porchlight .firebaserc)" = "1" && test "$(grep -c '^VITE_FIREBASE_API_KEY=.' apps/web/.env.local)" = "1" && firebase functions:secrets:access ANTHROPIC_API_KEY | cut -c1-7 | grep -q 'sk-ant-' && test -z "$(git status --porcelain | grep -E '\.env\.local|\.secret\.local')"</automated>
    <fails_when>.firebaserc missing or not pointing at a porchlight project; .env.local not filled; the Anthropic secret is absent or not a `sk-ant-` key; a secrets file shows up as untracked/modified in git</fails_when>
  </verify>
  <done>Project on Blaze with Firestore `(default)` in `nam5`, `.firebaserc` committed, `.env.local` filled, four secrets present in Secret Manager (one real, three placeholders), nothing secret tracked by git.</done>
</task>

<task type="auto">
  <name>Task 2: One-command deploy (hosting first, functions with Eventarc retry)</name>
  <files>.firebaserc</files>
  <read_first>
    - .planning/phases/01-foundation-and-landing-page-live/01-RESEARCH.md — "Pattern 6" step 6, "Pitfall 7", Assumption A1 (Cloud Build npm install fallback)
    - package.json (root `deploy` and `deploy:web` scripts)
    - firebase.json (Plan 01)
  </read_first>
  <precondition>Task 1 reported "project <id> ready": `.firebaserc` names the project, Blaze is active, the `(default)` Firestore database exists, and all four secrets exist in Secret Manager.</precondition>
  <action>
    1. Run `pnpm run deploy` (D-02: `pnpm -r build && firebase deploy --only hosting,functions,firestore:rules`). Expect 3-6 minutes on the first run while the CLI enables cloudfunctions / cloudbuild / artifactregistry / run / eventarc.
    2. If the functions step fails while hosting succeeded, do NOT loop blindly: (a) `Permission denied while using the Eventarc Service Agent` -> wait 2-3 minutes, re-run `firebase deploy --only functions` (max 3 attempts, ~10 minutes total); (b) a Blaze/billing error -> stop and return to Task 1 step 2; (c) a remote `npm install` failure in Cloud Build -> apply RESEARCH A1: from `functions/` run `npm install --package-lock-only --ignore-scripts` to emit `functions/package-lock.json` (commit it), then re-run `firebase deploy --only functions`; (d) `secret ... not found` -> return to Task 1 step 6.
    3. If functions are still failing after the retries, run `pnpm deploy:web` so the page is public regardless, record the failure output verbatim in the SUMMARY, and continue to Task 3's hosting checks; the callable checks will fail and be reported as a gap.
    4. Record from the CLI output: `Hosting URL: https://<id>.web.app` and the function URLs for `joinWaitlist` and `healthz`.
  </action>
  <acceptance_criteria>
    - `pnpm run deploy` output contains `Deploy complete!`
    - `curl -sI https://<id>.web.app | head -1` contains `200`
    - `firebase functions:list | grep -c 'joinWaitlist\|onWaitlistCreated\|healthz'` == 3
    - `curl -s https://us-central1-<id>.cloudfunctions.net/healthz` returns JSON containing `"ok":true` and `"states"` and no secret values (only `set`/`unset`)
    - `git status --porcelain | grep -c '.env.local\|.secret.local'` == 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm run deploy 2>&1 | tee /private/tmp/claude-502/-Users-bsmerbeck-git-hackathon/deploy.log | grep -q 'Deploy complete!' && ID=$(node -e "console.log(require('./.firebaserc').projects.default)") && curl -sI "https://$ID.web.app" | head -1 | grep -q 200 && curl -s "https://us-central1-$ID.cloudfunctions.net/healthz" | grep -q '"ok":true' && test "$(firebase functions:list | grep -c 'joinWaitlist\|onWaitlistCreated\|healthz')" = "3"</automated>
    <fails_when>deploy does not print `Deploy complete!` after the documented retries; hosting URL does not return 200; healthz does not answer with ok:true; fewer than the three functions are listed</fails_when>
  </verify>
  <done>Hosting, three functions, and rules are deployed from one command; the public URL returns 200; healthz confirms secrets are bound.</done>
</task>

<task type="auto">
  <name>Task 3: Live end-to-end proof — curl signup lands in Firestore, counter increments, page shows it</name>
  <files>.firebaserc</files>
  <read_first>
    - .planning/phases/01-foundation-and-landing-page-live/01-RESEARCH.md — "Code Examples §Calling the function with curl", "Verification Commands" (LAND-02/03/04 rows), "Security Domain" (rules test)
    - firestore.rules (Plan 01 — `stats/waitlist` is publicly gettable, everything else closed)
  </read_first>
  <precondition>Task 2 finished with `Deploy complete!` including functions (if functions are not deployed, record the gap and skip steps 2-4).</precondition>
  <action>
    Set `ID` from `.firebaserc`. Then:
    1. Page: `curl -s https://$ID.web.app | grep -c Porchlight` >= 1 (Plan 02 may or may not have deployed yet — the Plan 01 minimal page already has the title).
    2. Baseline: `curl -s "https://firestore.googleapis.com/v1/projects/$ID/databases/(default)/documents/stats/waitlist"` — publicly readable per rules; before any signup this returns a 404 NOT_FOUND JSON (doc does not exist yet), which is fine. Read `fields.count.integerValue` if present, else treat as 0.
    3. Signup via the documented envelope: `curl -s -X POST https://us-central1-$ID.cloudfunctions.net/joinWaitlist -H 'content-type: application/json' -d '{"data":{"email":"test@porchlight.dev","protecting":"my mom","ref":"verify"}}'` — expect `{"result":{"id":"..."}}` and no `"error"`. Then the negative case: same URL with `-d '{"data":{"email":"not-an-email"}}'` -> HTTP 400 with `"status":"INVALID_ARGUMENT"` (proves zod runs server-side).
    4. Wait up to 10 s, re-read `stats/waitlist` via the REST URL from step 2; `fields.count.integerValue` must be baseline + 1. Fallback if the REST read is rejected: the admin one-liner from RESEARCH Verification Commands (run from `functions/` with `GOOGLE_CLOUD_PROJECT=$ID`) on `doc('stats/waitlist')` and on the newest `waitlist` doc — confirm `ref` is `verify` and `email` is `test@porchlight.dev`.
    5. Rules negative test: `curl -s -X PATCH "https://firestore.googleapis.com/v1/projects/$ID/databases/(default)/documents/stats/waitlist" -H 'content-type: application/json' -d '{"fields":{"count":{"integerValue":"9999"}}}'` must return HTTP 403 `PERMISSION_DENIED` (anonymous write denied). Also `curl -s "https://firestore.googleapis.com/v1/projects/$ID/databases/(default)/documents/waitlist"` must return 403 (list denied).
    6. Browser: open `https://$ID.web.app/?ref=judges` in one tab and the same URL in a second browser/incognito; submit the form in the second; the first tab's counter must tick without reload. Verify in the first tab's DevTools that `sessionStorage['porchlight:ref'] === 'judges'`. This is a human-check step; report what was observed.
  </action>
  <acceptance_criteria>
    - `curl -s https://<id>.web.app | grep -c Porchlight` >= 1
    - joinWaitlist valid POST returns a body containing `"result"` and `"id"` and not containing `"error"`
    - joinWaitlist invalid POST returns `INVALID_ARGUMENT`
    - `stats/waitlist` REST read after the signup shows `count` == baseline + 1
    - Anonymous PATCH to `stats/waitlist` returns `PERMISSION_DENIED`; anonymous list of `waitlist` returns `PERMISSION_DENIED`
    - Human check: counter in tab A ticked when tab B submitted; sessionStorage ref captured as `judges`
  </acceptance_criteria>
  <verify>
    <automated>ID=$(node -e "console.log(require('./.firebaserc').projects.default)") && curl -s "https://$ID.web.app" | grep -q Porchlight && B=$(curl -s "https://firestore.googleapis.com/v1/projects/$ID/databases/(default)/documents/stats/waitlist" | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{try{console.log(Number(JSON.parse(s).fields.count.integerValue)||0)}catch{console.log(0)}})") && R=$(curl -s -X POST "https://us-central1-$ID.cloudfunctions.net/joinWaitlist" -H 'content-type: application/json' -d '{"data":{"email":"test@porchlight.dev","protecting":"my mom","ref":"verify"}}') && echo "$R" | grep -q '"result"' && ! echo "$R" | grep -q '"error"' && curl -s -X POST "https://us-central1-$ID.cloudfunctions.net/joinWaitlist" -H 'content-type: application/json' -d '{"data":{"email":"not-an-email"}}' | grep -q INVALID_ARGUMENT && sleep 8 && A=$(curl -s "https://firestore.googleapis.com/v1/projects/$ID/databases/(default)/documents/stats/waitlist" | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{try{console.log(Number(JSON.parse(s).fields.count.integerValue)||0)}catch{console.log(0)}})") && test "$A" -ge "$((B+1))" && curl -s -o /dev/null -w '%{http_code}' -X PATCH "https://firestore.googleapis.com/v1/projects/$ID/databases/(default)/documents/stats/waitlist" -H 'content-type: application/json' -d '{"fields":{"count":{"integerValue":"9999"}}}' | grep -q 403</automated>
    <fails_when>page lacks the title; the callable returns an error envelope or accepts an invalid email; the counter did not increase within ~8 s (trigger not firing — check `firebase functions:log --only onWaitlistCreated`); anonymous write is not rejected with 403</fails_when>
    <human-check>Two browsers on `https://<id>.web.app/?ref=judges`: submit in one, counter ticks in the other without reload; DevTools shows `sessionStorage['porchlight:ref'] === 'judges'`.</human-check>
  </verify>
  <done>A real signup traverses callable -> Firestore -> trigger -> counter on the live site; rules reject anonymous writes and lists; invalid payloads are rejected server-side; the counter ticks across browsers.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 4: Post the page with attributed refs, make the QR code, record the baseline count</name>
  <files>.firebaserc</files>
  <read_first>
    - .planning/phases/01-foundation-and-landing-page-live/01-CONTEXT.md (D-11 planned refs; "Specific Ideas" — post the page with refs is a human checkpoint)
    - /Users/bsmerbeck/git/hackathon/PLAN.md (traction slide expectations)
  </read_first>
  <action>
    The human posts `https://<id>.web.app/?ref=<ref>` with these exact refs (D-11): `li` (LinkedIn), `reddit`, `fb` (Facebook), `friends` (group texts / DMs). Generate a QR code for `https://<id>.web.app/?ref=qr` — `qrencode` is not installed; open `https://api.qrserver.com/v1/create-qr-code/?size=600x600&data=https%3A%2F%2F<id>.web.app%2F%3Fref%3Dqr` (or any QR generator) and save the PNG outside the repo (e.g. `~/Desktop/porchlight-qr.png`) for the stage/table. `judges` is reserved for the demo slide link.

    Before posting, note the current count from `https://firestore.googleapis.com/v1/projects/<id>/databases/(default)/documents/stats/waitlist` (this includes the test signups from Task 3; record it as the baseline in the SUMMARY so the traction slide can subtract test entries). Claude records: baseline count, timestamp, and which refs were actually posted.

    Then tell Claude: "posted: <list of refs>, baseline <n>".
  </action>
  <acceptance_criteria>
    - Human confirms at least `li` and `qr` were posted/generated (others optional if a platform is unavailable tonight)
    - SUMMARY records baseline count, timestamp, and refs posted
    - `curl -s "https://firestore.googleapis.com/v1/projects/<id>/databases/(default)/documents/stats/waitlist"` returns a `count` >= the Task 3 value
  </acceptance_criteria>
  <verify>
    <automated>ID=$(node -e "console.log(require('./.firebaserc').projects.default)") && curl -s "https://firestore.googleapis.com/v1/projects/$ID/databases/(default)/documents/stats/waitlist" | grep -q integerValue</automated>
    <fails_when>the stats doc cannot be read publicly (rules regression) or does not exist (trigger never fired)</fails_when>
    <human-check>Links with `?ref=li`, `?ref=reddit`, `?ref=fb`, `?ref=friends` posted; QR for `?ref=qr` saved; baseline count recorded.</human-check>
  </verify>
  <done>The page is in the wild with attributed links and a QR; the baseline count is recorded for the traction slide.</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces (Plan 03)

| Artifact | Purpose |
|----------|---------|
| `.firebaserc` (committed) | `{ "projects": { "default": "<id>" } }` — the only new tracked file |
| `apps/web/.env.local` (gitignored) | real `VITE_FIREBASE_*` values |
| `functions/.secret.local` (gitignored, optional) | local `ANTHROPIC_API_KEY` for the functions emulator |
| Secret Manager | `ANTHROPIC_API_KEY` (real), `TWILIO_ACCOUNT_SID`, `TWILIO_AUTH_TOKEN`, `ELEVENLABS_API_KEY` (placeholders) |
| Deployed surfaces | `https://<id>.web.app`, `joinWaitlist`, `onWaitlistCreated`, `healthz`, firestore rules |
| `functions/package-lock.json` (only if RESEARCH A1 fallback was needed) | makes Cloud Build's npm install deterministic |
| SUMMARY fields | live URL, function URLs, baseline count + timestamp, refs posted |
</artifacts>

<firebase_surfaces>
## Firebase surfaces used / not used

**Used:** Hosting (`<id>.web.app`); Functions v2 `onCall` / `onRequest` / `onDocumentCreated` in `us-central1`; `defineSecret` via `functions:secrets:set`; Firestore rules deploy; Firestore REST read of `stats/waitlist` (anonymous, rules-gated) for verification; web `onSnapshot` for the live counter.
**Not used:** Auth (no accounts); App Check (accepted risk tonight per D-10); Analytics (deferred; `?ref=` is the attribution); Remote Config (nothing to toggle); Firestore emulator (no JDK on this Mac — live project is the test bed per RESEARCH Pitfall 2).
</firebase_surfaces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet -> `joinWaitlist` / `healthz` | Public HTTPS endpoints |
| Internet -> Firestore REST | Anonymous reads gated only by rules |
| Human shell -> Secret Manager | Secret values typed at prompts, never as CLI args or files in the repo |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering / DoS | `joinWaitlist` spam via curl (now public) | medium | mitigate (partial) / accept | zod + `maxInstances: 5` from Plan 01; per-IP rate limiting and App Check accepted-not-mitigated tonight (D-10). Test signups recorded as baseline so the slide number is honest. |
| T-01-02 | Information disclosure | Secrets during bootstrap | high | mitigate | Values entered at `functions:secrets:set` prompts; `.env.local`/`.secret.local` gitignored (Plan 01 Task 4 proof); `healthz` reports `set`/`unset` only; Task 1 verify re-checks `git status` for secrets files. |
| T-01-03 | Elevation of privilege | Rules on the live database | high | mitigate | Task 3 step 5 proves anonymous PATCH and list return 403 against the deployed rules. |
| T-01-08 | Repudiation / accuracy | Counter double-increment on Eventarc retry | low | accept | At-least-once delivery may over-count rarely (RESEARCH Pitfall 8); reconcile from `waitlist` size if a judge asks. |
| T-01-SC | Tampering | Cloud Build remote `npm install` of `functions/package.json` | medium | mitigate | Exact pins; if A1 fallback is used, the generated `package-lock.json` is committed so the remote install is deterministic. |
</threat_model>

<verification>
- `pnpm run deploy` -> `Deploy complete!`; `curl -sI https://<id>.web.app` -> 200 (FND-01 deploy half, LAND-01 public URL)
- `firebase functions:secrets:access ANTHROPIC_API_KEY | cut -c1-7` -> `sk-ant-`; `git status --porcelain` shows no secrets files (FND-02)
- Callable valid -> `{"result":{"id":...}}`, invalid -> `INVALID_ARGUMENT`; `stats/waitlist.count` +1; `ref` stored (LAND-02, LAND-03, LAND-04)
- Two-browser tick observed by the human (LAND-03 "live")
- Refs posted + QR generated; baseline recorded
</verification>

<success_criteria>
- Public URL serves the app; three functions deployed; rules deployed and proven closed
- One real signup traversed the full path and the counter incremented on the live page
- Nothing secret is tracked; `.firebaserc` is the only new tracked file
- Attributed links and QR are out; baseline count recorded in the SUMMARY
</success_criteria>

<output>
Create `.planning/phases/01-foundation-and-landing-page-live/01-03-SUMMARY.md` when done — include live URL, function URLs, baseline count + timestamp, and refs posted.
</output>
