---
phase: 02-core-screening-loop
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: []
autonomous: false
requirements: []

estimate:
  tokens: 22000
  raw_tokens: 15000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Twilio and ElevenLabs accounts are active (out of trial), and all four vendor secrets Functions need are readable via Secret Manager with none in git"
    - "A real inbound call to the Porchlight number completes the full CALL-01..05 + RISK-01..03 loop end to end with no further code changes"
  artifacts: []
  key_links:
    - "Human phone -> Twilio number -> ElevenLabs agent -> deployed Functions (02-02) -> Firestore calls/{id} — the whole chain proven live at least once"
---

<objective>
Complete the account-level and secret-level prerequisites that 02-02's dashboard wiring depends on, then run the full live phone test script and confirm CALL-05's post-call persistence fields land correctly on a real call.

Purpose: 02-01 and 02-02 are fully buildable and unit-testable without any of this — this plan is the human-only gate between "code compiles and passes Vitest" and "a real phone call works," per the ROADMAP's 1AM go/no-go framing (DEMO-05 remains the fallback if any of this stalls).
Output: active, out-of-trial Twilio + ElevenLabs accounts; four Functions secrets set; one confirmed successful end-to-end phone call.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-screening-loop/02-RESEARCH.md
@.planning/phases/01-foundation-and-landing-page-live/01-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Twilio + ElevenLabs accounts, number, and Firebase secrets</name>
  <precondition>Firebase project created and Blaze-upgraded (Phase 1 Plan 03 human checkpoint) — `firebase use` must already point at a real project before `firebase functions:secrets:set` will work.</precondition>
  <action>
    1. Twilio: sign up at twilio.com (skip if Phase 1's human action already did this) -> Phone Numbers -> Buy a Number (US, Voice-capable) -> Billing -> upgrade out of trial (~$20) so calls carry no trial announcement -> Console -> Account -> API keys & tokens: copy the Account SID + Auth Token (or create a scoped API Key SID/Secret for least privilege, per 02-RESEARCH.md Pattern 1).
    2. ElevenLabs: sign up at elevenlabs.io (skip if Phase 1's human action already did this), choose Starter ($6/mo, 75 min) or Creator ($22/mo, 275 min) tier -> Profile -> API Keys: copy the API key.
    3. Generate a random shared secret for `ELEVENLABS_LLM_TOKEN` (any sufficiently random string — this is the value you will paste into BOTH the ElevenLabs agent's Custom LLM secret field in 02-02 Task 3 and the Firebase secret below).
    4. Run `firebase functions:secrets:set TWILIO_ACCOUNT_SID`, `firebase functions:secrets:set TWILIO_AUTH_TOKEN`, `firebase functions:secrets:set ELEVENLABS_API_KEY`, `firebase functions:secrets:set ELEVENLABS_LLM_TOKEN` — each prompts for a value; paste from steps 1-3 (per D-04's secrets pattern — Secret Manager only, never a tracked file).
    5. Confirm each secret is stored without ever printing its value into a committed file or shell history that gets saved.
  </action>
  <acceptance_criteria>All four secrets exist in Firebase Secret Manager and are readable by Functions; neither account is in trial mode; a US voice-capable Twilio number is owned.</acceptance_criteria>
  <verify>
    <automated>firebase functions:secrets:access TWILIO_ACCOUNT_SID; firebase functions:secrets:access TWILIO_AUTH_TOKEN; firebase functions:secrets:access ELEVENLABS_API_KEY; firebase functions:secrets:access ELEVENLABS_LLM_TOKEN</automated>
    <fails_when>any of the four commands exits non-zero or reports the secret does not exist</fails_when>
  </verify>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: Live phone test script + CALL-05 persistence check</name>
  <precondition>02-02 Task 3 (ElevenLabs dashboard wiring) and this plan's Task 1 (secrets) are both complete.</precondition>
  <action>
    From your own phone (not a Twilio-trial-flagged number), dial the Porchlight number. Confirm the greeting plays within ~2 rings. Speak each `JAIL_SCRIPT` line (from `@porchlight/shared`, 02-01) in order, pausing ~2s between lines, watching the Firestore console's `calls/{id}` doc update live. After the "it's me, Brenden" line, confirm `state` flips to `verifying` and `verification.memberId==='brenden'`. End the call either by letting the AI's own `end_call` tool fire (if `recommendedAction` reaches `end`) or by invoking `forceVerdict({callId, verdict:'scam'})` (02-02) from the Firebase emulator/console — confirm a spoken line plays before hangup. After hangup, re-check the doc: confirm `endedAt`, `outcome`, and the full `turns[]` array are present and non-empty (CALL-05 post-call persistence). Redial once more to confirm the pipeline is repeatable (a lightweight rehearsal ahead of Phase 5's full 3x-in-a-row bar).
  </action>
  <acceptance_criteria>The full call completes with greeting, live turns, climbing risk, a verifying trigger, and a spoken end — and the post-hangup doc has `endedAt` + `outcome` + a complete `turns[]` array.</acceptance_criteria>
  <verify>
    <automated>curl -s "https://firestore.googleapis.com/v1/projects/&lt;project&gt;/databases/(default)/documents/calls/&lt;id&gt;" -H "Authorization: Bearer $(gcloud auth print-access-token)" | grep -c '"endedAt"\|"outcome"'</automated>
    <fails_when>the grep count is less than 2 (either field missing — the call was never finalized)</fails_when>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human operator's terminal/browser -> Firebase Secret Manager / ElevenLabs+Twilio dashboards | Secrets are typed by hand into vendor consoles and the `firebase` CLI; no code path in this plan touches them |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-10 | Information Disclosure | Secrets pasted during manual setup | medium | mitigate | Values go directly into Firebase Secret Manager and vendor dashboards only — never into a tracked file, `.env.local`/`.secret.local` (untracked, per D-04), or shell history that gets committed; confirm with `git status`/`git diff` before any commit in this session |

</threat_model>

## Artifacts this phase produces
None — this plan is human-checkpoint and live-verification only; it modifies no tracked files.

## Firebase/ElevenLabs/Twilio surfaces used / not used
- **Firebase used:** Secret Manager (`firebase functions:secrets:set`/`access`) for all four vendor secrets.
- **ElevenLabs used:** account/billing tier selection only in this plan (dashboard agent config itself is 02-02 Task 3).
- **Twilio used:** account signup, number purchase, trial upgrade, and Account SID/Auth Token (or API Key) retrieval.
- **Not used:** no new code surfaces — this plan is entirely account/secret/verification work.

<verification>
- All four Functions secrets exist and are readable (Task 1).
- A real end-to-end phone call exercises the greeting, live transcript, risk climb, verification trigger, and finalized post-call doc (Task 2).
</verification>

<success_criteria>
- Twilio and ElevenLabs are both fully account-ready (out of trial, correct tier) with no secret ever committed to git.
- At least one real phone call has proven the entire Phase 2 loop end to end, matching every ROADMAP Phase 2 success criterion.
</success_criteria>

<output>
Create `.planning/phases/02-core-screening-loop/02-03-SUMMARY.md` when done
</output>
</content>
