---
phase: 04-family-verification-and-dashboard
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - apps/web/src/pages/AppPlaceholder.tsx
  - apps/web/src/pages/Stage.tsx
  - apps/web/src/lib/outcomeBadge.ts
  - apps/web/src/lib/outcomeBadge.test.ts
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03]

estimate:
  tokens: 50000
  raw_tokens: 33000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "/app shows the currently active (non-ended) call live — streaming transcript, risk meter, detected tactics, claimed identity, and state — updating with no reload as households/demo/feed/{callId} changes"
    - "/app shows call history with Verified / Scam blocked / Screened outcome badges and opens any call's full transcript (and report text once Phase 5 adds it)"
    - "/stage renders exactly one call in large type on a dark background, readable from the back of a room, driven by the same feed mirror"
    - "Neither page ever reads calls/{id} directly — only the households/demo/feed/{callId} public mirror (04-01)"
  artifacts:
    - apps/web/src/pages/AppPlaceholder.tsx
    - apps/web/src/pages/Stage.tsx
    - apps/web/src/lib/outcomeBadge.ts
  key_links:
    - "households/demo/feed onSnapshot(query, orderBy startedAt desc) -> Dashboard live panel + history list"
    - "households/demo/feed/{callId} onSnapshot -> Stage single-call view"
---

<objective>
Turn the two existing Phase 1 page skeletons into the real family dashboard and stage view: `/app` (D-08's reserved dashboard route, currently `AppPlaceholder.tsx`) shows the live call and history with outcome badges, and `/stage` (already scaffolded per `04-RESEARCH.md`) becomes the projector-friendly single-call view — both reading only `households/demo/feed/{callId}` (04-01's public mirror), never `calls/{id}` directly.

Purpose: this is the family-facing payoff of the whole phase — DASH-01/02/03 complete the "family watches the live transcript, risk meter, and history" half of the phase goal.
Output: `AppPlaceholder.tsx` and `Stage.tsx` rewritten in place (same filenames/routes Phase 1 already wired into `App.tsx` — this plan does not touch `App.tsx` at all, avoiding a same-wave file conflict with `04-02`, which owns the new `/verify` route).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-family-verification-and-dashboard/04-RESEARCH.md
@.planning/phases/01-foundation-and-landing-page-live/01-CONTEXT.md
@.planning/phases/04-family-verification-and-dashboard/04-01-PLAN.md
@apps/web/src/pages/AppPlaceholder.tsx
@apps/web/src/pages/Stage.tsx
@apps/web/src/pages/Sim.tsx
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Dashboard tracer — /app subscribes to households/demo/feed and renders one active call end-to-end</name>
  <files>apps/web/src/pages/AppPlaceholder.tsx</files>
  <precondition>04-01's `households/{householdId}/feed/{callId}` mirror and firestore.rules are deployed.</precondition>
  <read_first>04-RESEARCH.md architecture diagram + Pattern 3 (feed doc rules/shape), the locked D-05 `calls/{id}` shape (turns/risk/verification/outcome fields) from `01-CONTEXT.md`; `apps/web/src/pages/Sim.tsx` for the established `onSnapshot` + Firestore-import convention; current `AppPlaceholder.tsx` content before rewriting it.</read_first>
  <action>
    Rewrite `AppPlaceholder.tsx` in place (same filename, same `/app` route already wired in `App.tsx` per D-08 — do not touch `App.tsx`) to `onSnapshot(query(collection(db,'households/demo/feed'), orderBy('startedAt','desc')))` and render: (a) a live panel for the most recent call whose `state` is not `ended`/`verified`/`scam` (or, if none is active, the single most recent call) showing the streaming `turns[]` transcript (caller/assistant lines in order), a risk meter (`risk.score` as a numeric/bar indicator), the detected `risk.tactics[]` as chips, `risk.claimedIdentity`, and the current `state` as a badge — all driven by the one `onSnapshot` listener, no polling; (b) below it, the remaining calls (or all calls, once none are active) as a history list, each row showing `from`, `startedAt`, and a placeholder for its outcome (badge logic completed in Task 2). Read only `households/demo/feed/*` — never `calls/*` directly (per D-10's closed-rules design and RESEARCH's Anti-Patterns).
  </action>
  <verify>
    <automated>pnpm --filter web build && pnpm --filter web exec tsc --noEmit && grep -c "households/demo/feed" apps/web/src/pages/AppPlaceholder.tsx && grep -qc "doc(db, *'calls'" apps/web/src/pages/AppPlaceholder.tsx; test $? -ne 0</automated>
    <fails_when>the build/typecheck fails, the feed-collection grep returns 0 (not reading the mirror), or the file contains a direct `calls/` doc read (violates the closed-rules design)</fails_when>
  </verify>
  <acceptance_criteria>`/app` renders a live transcript/risk/tactics/claimedIdentity/state panel for the active call and a history list below it, both from one `onSnapshot` on the feed collection; no direct `calls/{id}` read anywhere in the file.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Outcome badges + transcript/report detail view (DASH-02 completion)</name>
  <files>apps/web/src/pages/AppPlaceholder.tsx, apps/web/src/lib/outcomeBadge.ts, apps/web/src/lib/outcomeBadge.test.ts</files>
  <read_first>REQUIREMENTS.md DASH-02 ("Verified / Scam blocked / Screened" badge language); the locked D-05 `outcome?: 'verified'|'scam'|'screened'` and `report?: string` fields.</read_first>
  <behavior>
    - Test: `outcomeBadge('verified')` returns `{label:'Verified', tone:'success'}`.
    - Test: `outcomeBadge('scam')` returns `{label:'Scam blocked', tone:'danger'}`.
    - Test: `outcomeBadge('screened')` returns `{label:'Screened', tone:'neutral'}`.
    - Test: `outcomeBadge(undefined)` returns `{label:'In progress', tone:'neutral'}` (an active, not-yet-ended call).
  </behavior>
  <action>
    Create `apps/web/src/lib/outcomeBadge.ts` exporting `outcomeBadge(outcome?: string): {label: string; tone: 'success'|'danger'|'neutral'}` per the `<behavior>` block; write `outcomeBadge.test.ts` against it. Extend `AppPlaceholder.tsx`'s history list rows to render each call's badge via `outcomeBadge(call.outcome)`, and make each row clickable to open (inline expansion or a simple modal, consistent with the app's existing shadcn conventions) the full `turns[]` transcript for that call plus `call.report` text when present, or "Report pending" when `report` is absent (Phase 5 adds report generation later — do not stub a fake report string here).
  </action>
  <verify>
    <automated>pnpm --filter web test -- outcomeBadge && grep -c "outcomeBadge" apps/web/src/pages/AppPlaceholder.tsx</automated>
    <fails_when>the Vitest cases fail, or `AppPlaceholder.tsx` never imports/uses `outcomeBadge` (badges not wired into the history list)</fails_when>
  </verify>
  <acceptance_criteria>Every history row shows the correct outcome badge and opens its own transcript (and report, when present) on click; an in-progress call shows a neutral "In progress" badge rather than a misleading outcome.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Stage view — projector-friendly single-call display (DASH-03)</name>
  <files>apps/web/src/pages/Stage.tsx</files>
  <read_first>04-RESEARCH.md ("/stage page pattern (skeleton already exists), same feed doc, CSS-only meter"); ROADMAP.md Phase 4 success criterion 5 (large type, dark background, readable from the back of the room); current `Stage.tsx` skeleton content before rewriting it.</read_first>
  <action>
    Rewrite `Stage.tsx` in place (same filename/route Phase 1 already wired) to `onSnapshot` the single most-recent non-`ended` call in `households/demo/feed` (falling back to the most recent overall if none are active) and render it in large type on a dark background per the phase's projector requirement: `claimedIdentity` and `state` prominently, a CSS-only risk meter (a `div` whose `width` is `${risk.score}%` and whose background color ramps green→amber→red by score threshold — no charting library, per RESEARCH's Don't Hand-Roll spirit of using plain platform primitives for anything this simple), and the streaming transcript auto-scrolled to the latest turn. On `state:'verified'`/`state:'scam'`, replace the live panel with a full-bleed colored banner ("VERIFIED" green / "SCAM BLOCKED" red) for a few seconds before returning to the live/history view — read only `households/demo/feed/*`, never `calls/*` directly.
  </action>
  <verify>
    <automated>pnpm --filter web build && pnpm --filter web exec tsc --noEmit && grep -c "households/demo/feed" apps/web/src/pages/Stage.tsx</automated>
    <fails_when>the build/typecheck fails, or the feed-collection grep returns 0 (Stage.tsx is not reading the mirror)</fails_when>
  </verify>
  <acceptance_criteria>`/stage` renders exactly one call in large type on a dark background with a live CSS-only risk meter and auto-scrolling transcript; verified/scam states show a full-bleed colored banner; no direct `calls/{id}` read anywhere in the file.</acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces (Plan 03 — dashboard + stage)
- `apps/web/src/pages/AppPlaceholder.tsx` — rewritten in place as the real family dashboard at `/app` (D-08).
- `apps/web/src/pages/Stage.tsx` — rewritten in place as the projector stage view at `/stage`.
- `apps/web/src/lib/outcomeBadge.ts` — pure outcome→badge mapping, unit-tested.

## Firebase surfaces used / not used
- **Firebase used:** `onSnapshot` on `households/demo/feed` (public-read collection + document reads, 04-01).
- **Firebase not used:** Firebase Auth, any direct `calls/{id}` read, any write path (both pages are read-only consumers of 04-01's mirror).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Any internet client → `households/demo/feed` (public `get`/`list`) | Deliberately public-read by design (RESEARCH Assumption A4) — no per-family auth exists in this hackathon build |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-07 | Information Disclosure | `households/demo/feed/{callId}` full-call public mirror (transcript text, claimed identity) | low | accept | Explicitly accepted per this phase's scope: contains no PII beyond first names and the transcript text of the demo call; a real product would need per-family auth (RESEARCH A4) |
| T-04-08 | Tampering | Dashboard/Stage write paths | low | mitigate | Both pages are read-only `onSnapshot` consumers — no `set`/`update`/`add` call exists anywhere in either file; `firestore.rules`' `allow write: if false` on the feed collection (04-01) is the actual enforcement point |

</threat_model>

<verification>
- `pnpm --filter web build` and `pnpm --filter web exec tsc --noEmit` exit 0 for both pages.
- `pnpm --filter web test -- outcomeBadge` exits 0.
- Both `AppPlaceholder.tsx` and `Stage.tsx` read only `households/demo/feed/*`, never `calls/*`.
</verification>

<success_criteria>
- `/app` shows the live call (transcript, risk meter, tactics, claimed identity, state) and a history list with correct outcome badges that open a transcript/report detail view.
- `/stage` renders a single call, large type, dark background, readable from the back of a room, with a live CSS-only risk meter.
</success_criteria>

<output>
Create `.planning/phases/04-family-verification-and-dashboard/04-03-SUMMARY.md` when done
</output>
